# 04_RUNTIME — NOT RUN

Runtime/game execution is explicitly OUT OF SCOPE for
PE-NIF-HASH-PRIMITIVE-REVALIDATION-R3 (per the binding prompt: READ-ONLY
boundaries on original game files, historical runs, R2, wiki/docs/nif,
canonical, PE_AUTO_LOOP, shared tools, runtime code; "no game/Ghidra").

NOT executed in this run:
- no PE.exe / Entropia.exe / any game client,
- no Ghidra / x32dbg / Frida / apitrace session,
- no emulator or compatibility layer,
- no historical run driver (no R29-R40 driver, no R1/R2 control script — the
  R2 script was NOT executed; ONLY its extracted literal helper declarations
  ran as pure functions inside a Node vm context, hash-pinned),
- no morph-boundary research, no leaf/XYZ, no MAPRE or milestone work,
- no PE_AUTO_LOOP relaunch or modification, no wiki application, no canonical
  or vault update, no runtime-code change.

This run is a bounded evidence-revalidation package: hash-primitive known-answer
testing, per-entry value identity and mismatch census across both hash-pinned
Models.bnt corpora (read-only), corrected census recount, historical re-sums
(R34/R35/R2 artifacts), sidecar byte reconstruction, three-state gate
machinery, and one bounded documentation publication. Nothing here promotes
any runtime-gated claim; every claim stays at its run-derived evidence level.
