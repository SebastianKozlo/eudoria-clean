# RUN_CONTRACT — PE_935_NINODE_SLOT17_GAMEBRYO_ORACLE_MINICHECK_R1_20260914

## Identity

- RUN_ID: `PE_935_NINODE_SLOT17_GAMEBRYO_ORACLE_MINICHECK_R1_20260914`
- TARGET: PCG_9_3_5 / Entropia Universe 9.3.5 client binary
- RUN_CLASS: MATERIAL (bounded static mini-check). STATIC-ONLY — the client never ran.
- Executor: pe-reconstruction, direct PE-MASTER dispatch, NO_NESTED_TASKS.
- Publication: branch `audit/pe935-ninode-slot17-gb-oracle-minicheck-r1` (path-limited
  commit of this package only, then push; no master merge, no AUDIT_ENTRYPOINT change).

## Pinned identities (fail-closed, all re-verified in the completing session)

| Item | Path | Size | SHA256 |
|---|---|---|---|
| Entropia.exe | `D:\Eudoria_Reconstruction\pcg_install\Entropia.exe` | 8015872 | `E7785430E81DFFE648CE8F5312414B17BC9FCE61389689A22F753765D5280F31` |
| gb12_oracle.exe (identified by SHA256 only; banner STALE) | `D:\gamebyroengine\extracted\gb12_build\bin\gb12_oracle.exe` | 594944 | `DD7112A41D83557395C5D31F72C89BFEA3D7906AB28EF4DD3C4BD8E398EB046C` |
| BASE_SHA = origin/master = remote master = worktree HEAD | repo `12_WebGame/eudoria-clean` | — | `3644e5ac9cbf7b5445861e7f5342fb8642741346` |

Gamebryo oracles (read-only): GB 1.1.2 Evaluation (`D:\gamebyroengine\Gamebryo 1.1.2 Evaluation\`
— installed form of the pinned 1.1.2 evaluation package; also `D:\gamebyroengine\extracted\Gb112_eval\`,
`gb112_tools_setup\`, `Gb112_docs_html\`), GB 1.2.2.6 semantic source
(`D:\gamebyroengine\extracted\Gb12_Source\`), GB 1.2.2.6 build products
(`D:\gamebyroengine\extracted\gb12_build\`). Full per-file SHA256 pins:
`00_Control/SOURCE_IDENTITIES.json`.

## Research questions

- A: exact Gamebryo class/virtual map for NiRefObject->NiObject->NiObjectNET->NiAVObject->NiNode
  in 1.1.2 and 1.2.2.6, by TWO independent methods (inheritance-aware header reconstruction +
  compiled .obj/.lib relocation evidence); NiNode slot17 identity in each version.
- B: GetObjectByName behavioral fingerprint (compact) in 1.1.2 and 1.2.2.6, with exact deltas.
- C: Entropia 0x007B5390 static decode from physical bytes only (no WIP package input).
- D: ABI + behavior comparison GB112 / GB12 / Entropia across the contracted dimensions,
  MATCH/MISMATCH/UNKNOWN, non-circular (each side measured from its own bytes).
- Secondary: returned_object +0x90 in GB112 and GB12 SEPARATELY; Entropia +0x90 was
  UNKNOWN unless proven from Entropia itself — it WAS proven this run (bounded
  single-slot probe; see OFFSET90_ORACLE.md).
- Negative controls NC1/NC2/NC3; status algebra; model-bridge guard; gates G0..G9.

## Rules honored

- Worktree isolation: `D:\Eudoria_Reconstruction\worktrees\PE_935_NINODE_SLOT17_GB_ORACLE_MINICHECK_R1\`
  on branch `audit/pe935-ninode-slot17-gb-oracle-minicheck-r1` at BASE_SHA. Master untouched;
  main working tree untouched (dirty only with the concurrent LINK30 session's edits);
  untracked WIP packages `docs/audits/PE_935_NINODE_SLOT17_FIRSTCALL_R1_20260914/` and
  `experiments/` neither read nor modified.
- Section-2 known Entropia facts were INPUT ONLY, never upgraded. The ENTROPIA decode in
  this run was re-measured from the physical EXE by this run's own scripts (vtable walk,
  RTTI chain, both disassemblies, neighbor slots, NiRTTI static-init probe), independently
  of the LINK30/firstcall WIP packages.
- TRANSFORM_TO_MODEL was NOT upgraded by any Gamebryo source identity.
- No Gamebryo source/binary files, no Entropia payload, no NIF/BNT payload entered the repo.
  Derived evidence only: facts, hashes, paths, line ranges, mangled symbol names,
  disassembly listings, derived pseudocode, small essential snippets.
- Deterministic recomputation scripts live only under `00_Control/` and `03_EVIDENCE/`.

## Pre-existing worktree state (honesty note)

The contracted worktree+branch already existed at BASE_SHA at the completing session's
start (branch pre-created; no extra commits). It contained a partial, untracked,
uncommitted set of files for THIS SAME RUN_ID produced by TWO earlier interrupted
sessions of this same assignment (file times 2026-09-14 13:46-14:12 and 14:18-14:28
local; the second session itself recorded the first as its own pre-existing state).
The completing session (start 14:31 local) did NOT adopt any prior file on trust:
- every analysis document (02_ANALYSIS/*) was rewritten by the completing session;
- every evidence file (03_EVIDENCE/*) and raw listing (01_RAW/ENTROPIA_007B5390_DISASM.txt)
  was REGENERATED by re-executing the deterministic generator scripts in this session
  against the pinned physical sources;
- every oracle hash pin was re-measured and matched;
- the completing session additionally decoded the vtable ABI prefix (slots 0/1/2),
  neighbor slots 16/18, slot 27 (UpdateWorldData) and the NiRTTI static initializer —
  evidence the prior sessions did not have — which upgraded the Entropia +0x90 answer
  from UNKNOWN to measured and strengthened the ABI comparison.

## HARD STOP

After successful push of the audit package branch: stop. No next experiment, no scope
expansion, no master mutation.
