# 03_EVIDENCE — regenerated evidence files

ALL files in this directory were REGENERATED by the completing session of
PE_935_NINODE_SLOT17_GAMEBRYO_ORACLE_MINICHECK_R1_20260914 by executing the
deterministic generator scripts in `00_Control/` against the pinned physical
sources (SHA256 pins in `00_Control/SOURCE_IDENTITIES.json`, re-measured this
session). Nothing here is adopted on trust from the earlier interrupted sessions
of this same run; any prior copies were overwritten by fresh executions.

## Generators (deterministic; same input -> same output)

- `00_Control/entropia_rtti_probe.py` (stdlib only; fail-closed SHA pin) ->
  `ENTROPIA_RTTI_CHAIN_PROBE.json`
- `00_Control/entropia_disasm_7b5390.py` (capstone 5.0.7 via the documented
  PYTHONPATH; fail-closed SHA pin) -> `ENTROPIA_SLOT_NEIGHBORS_DISASM.txt` sections
  and `01_RAW/ENTROPIA_007B5390_DISASM.txt`
- `00_Control/parse_coff_vtable.py` (stdlib only) ->
  `GB112_NIMAIN_LIB_VTABLE_DUMP.json`, `GB12_NINODE_OBJ_VTABLE_DUMP.json`,
  `GB12_CHAIN_OBJ_VTABLE_DUMP.json`
- `00_Control/coff_disasm_symbol.py` (capstone) -> the `GB112_*_DISASM.txt` and
  `GB12_*_DISASM.txt` files

Two files contain hand-extracted BYTE FACTS (not script-generated) with exact VA
locators so any auditor can re-derive them with a hex viewer:
`ENTROPIA_NIRTTI_STATIC_INIT.txt` (initializer bytes at 0x00A6C200, string at
0x00A8CE00, PE section table) — every quoted byte sequence was re-read from
the physical file in this session.

## Provenance and hygiene

- No Gamebryo source or binary file, no Entropia payload, no NIF/BNT payload is
  included; all files are derived facts (hashes, paths, mangled symbol names,
  relocation-mapped vtable dumps, disassembly listings of the specific functions
  under audit, derived pseudocode).
- Oracle disassembly listings cover ONLY the audited functions (constructors,
  GetObjectByName pair, UpdateWorldData) — no wholesale source reproduction.
- `EVIDENCE_INDEX.csv` lists each file with its generator, its physical input and
  its role in the gates.
