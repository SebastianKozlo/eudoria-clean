# PE_MASTER_REVIEW.md — PE_935_SCENEFEEDER_LINK30_IDENTITY_R1_20260914

PE_MASTER_REVIEW
AUDITED_RUN      = PE_935_SCENEFEEDER_LINK30_IDENTITY_R1_20260914 (executor pe-reconstruction, direct PE-MASTER dispatch; formalization = the human's contract relayed verbatim by PE-MASTER — with one PE-MASTER dispatch defect: the contract's "TD+0x0C" RTTI name-offset label, corrected by amendment R1, see GATE G4 note; INTERNAL_QC = fresh-context pe-master-auditor, QC_AUDIT_R1.md, QC_PASS 0xP0 / 0xP1 / 3xP2 / 6xP3; pre-publication amendment batch R1 applied and independently verified. Commit range: BASE 1a490eed4ca2b295e78cd3cf851a08ac9c93930b -> this publication commit.)
VERDICT          = MASTER_ACCEPTED (advisory)
AUTHORITY_STATUS = ADVISORY_PRE_QUALIFICATION — PE-MASTER_STATUS remains PROVISIONAL_UNTIL_QUALIFIED (no human Q1 PASS recorded; PROJECT_OPERATING_MODEL section 12)
CANONICAL_GATE_EFFECT = NONE
SNAPSHOT_STATE   = Audit start: HEAD == origin/master == ls-remote == 1a490eed4ca2b295e78cd3cf851a08ac9c93930b (BASE verified), git status = foreign untracked experiments/ only; PE_MASTER_LOOP_STATE carried a STALE dead state (loop 89968363, deadline 2026-09-07) — plugin-owned, untouched, no live controller. Final pre-publication: HEAD unchanged == BASE; dirty = this package (untracked, by design) + experiments/. Publication = path-limited commit + push (this commit) by pe-master-auditor; post-publication verification (commit SHA, path census, remote equality, entrypoint row survival) performed by PE-MASTER and recorded in the session report to the human.
COVERAGE         = FULLY READ by PE-MASTER: RUN_CONTRACT.md (verbatim human order present incl. the administrative correction 27/26/28), REPORT.md (pre- and post-amendment), HANDOFF.md, STAGE_ACCEPTANCE_GATES.csv, QC_AUDIT_R1.md (426 lines), census.py + finalize.py in full (generator/predicate audit), SF30_PROVENANCE.md, SF30_RTTI_RAW.txt (pre+post), POSITIVE_CONTROL_0050A050.txt, AMEND_LOG_R1.md (425 lines), raw derivation header sections 01-05 + 2.1-2.6. CENSUS-LEVEL (own tools): SF30_WRITER_CENSUS.csv all 3643 rows (recount, classification groups, PROVEN row contents, UNATTRIBUTED=15 checked conservative); SF30_WRITER_RAW.txt (3643-block count; all 3023 REJECTED why-lines parsed to the fired-reason distribution; sweep stats; amended-line verification); manifest re-hash 17/17; SCRIPT_SHA256 4/4; full-package forbidden-label census with line-by-line adjudication; post-amendment leftover-label census; census_state.json. PHYSICAL (AUDITOR_COUNTERCHECK, own byte reads outside the project tree): 47 VA byte-windows (both writers, full P1/P2 provenance hop chains, positive-control window, block-ctor vtable stores incl. the secondary @0x7B605E, operator-new thunk -> IAT -> "??2@YAPAXI@Z"), all three RTTI chains (calibration .?AVSceneFeederObject@@; primary .?AVNiNode@@; secondary ?$NiTPointerList@PAVNiDynamicEffect@@@@ — every dword + name bytes), slot-17 dword [0xA8CD38]=0x007B5390, NiNode vtable extent 47, SF vtable slots 0-5, byte-pattern counts (dword 0x00A7D458 occurs EXACTLY 2x in the whole file, both .text imm operands @0x509369/0x50A26B; dword 0x00509330 absolute refs = 0 in the whole file), E8-caller scans (SF ctor callers exactly {0x47D043, 0x52480F}; FUN_005247C0 create = 7 callsites; FUN_005094C0 = 15 callers incl. the 4 proven-load sites), REJECTED-reason distribution re-derived = 3023 exact. NOT CHECKED by PE-MASTER personally: 606 of the 618 POSSIBLE_ALIAS rows beyond QC's 12-row sample (class-level bound accepted; per-row non-resolution is the contract-designed residue); 100 of the 104 R-CTOR-OTHER rows beyond QC's 4 full-body deep-checks; the 64 sweep restart points individually; ANY decode of 0x007B5390 / 0x437F70 / 0x82B5A0 bodies (forbidden; extent-only boundary derivations by the gate machinery are disclosed, semantics-free); any runtime behavior (STATIC-ONLY held — the client never ran); the containing regions of the 15 UNATTRIBUTED census rows (classification branch verified conservative: non-esp/ebp unattributed rows fall to POSSIBLE_ALIAS); the prior SLOT_CENSUS package's science beyond the referenced artifacts + administrative counts.
STATUS_ALGEBRA  = CLAIM_KNOWLEDGE_STATUS: the two writers + their creation/receipt provenance + the RTTI type identity + the next-seam value = CONFIRMED (physical byte level, era PCG_9_3_5, STATIC-ONLY); census enumeration-completeness = CONFIRMED (two independent sweep implementations reproducing the exact candidate set + PE-MASTER pattern cross-checks); "exactly two PROVEN writers" = CONFIRMED as worded; GLOBAL exclusivity of the writer set = NOT CLAIMED (the 618-row residue is disclosed at every level); "the object at SF+0x30 at slot-3 read time is a NiNode" = STRONGLY_SUPPORTED as the canonical lifecycle value (ctor-stored, dtor-released+null; mid-life interprocedural writers bounded-not-excluded). FINDING_DISPOSITION: QC P2-1 ACCEPTED (VA-citation form corrected; the 4 load/call splits byte-verified by PE-MASTER); P2-2 ACCEPTED with PE-MASTER SELF-ATTRIBUTION (the wrong "TD+0x0C" label originated in PE-MASTER's own dispatch contract — the executed walker always read +0x08, calibration-proven; corrected to TD+0x08 by amendment; QC's location list is noted as slightly over-inclusive re REPORT.md which never carried the label — no contradiction); P2-3 ACCEPTED as a gate-design finding for FUTURE gate templates only (this run's G5 result is independently TRUE — PE-MASTER's own full-package census adjudicated every occurrence: zero claim-context; the permissive detector was deliberately NOT amended in-run per the QC verdict); P3-4, P3-5, P3-6, P3-7, P3-8, P3-9 ACCEPTED (all fixed in amendment R1); P3-10 ACCEPTED (PE-MASTER's own canon-completeness finding: REPORT section 2 BEFORE now credits the accepted POSITION_CONSTRUCTION prior association — fixed). EXECUTABLE_GATE_STATE: G0-G5 PASS (recomputed at gate time; independently confirmed by QC; G1/G4 underlying bytes re-verified physically by PE-MASTER; G5's script census scope post-amendment = the EXECUTOR set, with the QC files covered by QC_AUDIT_R1.md C7 and by this review's own full-package census — combined coverage complete; disclosed in AMEND_LOG_R1.md 8.2). HUMAN_REVIEW_STATE: PENDING (this report to the human; Desktop post-audit next per the human's flow). PERSISTENCE_STATE: package complete on disk (31 files) at review time; publication = this commit (path-limited package + AUDIT_ENTRYPOINT.md only). APPLICATION_STATE: n/a (no live-document application proposed by this run; the only doc target = the AUDIT_ENTRYPOINT LATEST RUNS row, target-mapped by the persistence worker, +1/-0). MILESTONE_STATE: EU935-M1 continues; no milestone action taken or implied; TRANSFORM_TO_MODEL remains NOT_DEMONSTRATED.
CLAIM_MATRIX    =
1. CL-SF30-WRITERS — exactly two PROVEN static writers of [SF+0x30]: 0x005093C3 in SF ctor FUN_00509330 (stores the freshly allocated new(0x118) block; bytes 89 45 30) and 0x0050A2D1 in SF dtor body FUN_0050A240 (stores 0 after the refcount-release protocol; bytes 89 5E 30). STATUS: CONFIRMED. MEASURED: byte windows + single-definition receiver registers (ebp <- ecx @0x509357 / esi <- ecx @0x50A263, anchored by the SF vtable stores @0x509366 / 0x50A269). NUMERATOR: 2 proven rows. DENOMINATOR: 3643 candidates (documented enumeration rule over full .text; QC's independent re-implementation reproduced the sweep exactly; PE-MASTER pattern scans corroborate at the .text scale). PRIMARY_EVIDENCE: 02_ANALYSIS/SF30_WRITER_CENSUS.csv + 01_RAW/SF30_WRITER_RAW.txt + 02_ANALYSIS/SF30_PROVENANCE.md. INDEPENDENT_VALIDATION: QC own sweep + byte checks; PE-MASTER physical byte reads. WHY_NON_CIRCULAR: every claim re-derived from the hash-pinned EXE by three lineages (executor, QC, PE-MASTER); no generated summary trusted. ASSUMPTION_LINEAGE: capstone 5.0.7 WRITE-access classification shared by executor+QC (both proven writers are plain mov forms — unaffected); function-map attribution heuristic (15 UNATTRIBUTED rows classify conservatively). ALTERNATIVE_EXPLANATIONS: an unmodeled interprocedural SF-pointer channel could carry an SF pointer into a non-F_SF function (a 618-row residue) — NOT EXCLUDED, honestly bounded and disclosed; the claim "exactly two" is scoped to PROVEN writers. FALSIFIER: any additional census row whose receiver register is proven SF-this. FAILURE_CASE_DETECTED: the classifier demonstrably rejects (3023 per-row reasons) and the residue falls to POSSIBLE, never to false REJECTED. ROSETTA_LEVEL: UNKNOWN (not established in this run's scope). MISSING_LINKS: evidence-ladder rungs 6-7 (runtime causal / original-client parity) — STATIC-ONLY by contract. BLAST_RADIUS_SOURCE: the writer set + the residue feed the NEXT_SEAM design only.
2. CL-SF30-TYPE — the block stored at SF+0x30 at construction has primary vtable 0x00A8CCF4 with RTTI name .?AVNiNode@@ (the NetImmerse NiNode scene-graph node class), refcount dword at block+4, secondary embedded vtable 0x00A8CCE0 (?$NiTPointerList@PAVNiDynamicEffect@@@@) at block+0xE0. STATUS: CONFIRMED. MEASURED: store bytes c7 06 f4 cc a8 00 @0x7B6041; [0xA8CCF0]=0x00AAEEC8; COL+0xC ptd=0x00B936C8; name bytes at TD+0x08 = ".?AVNiNode@@"; secondary store @0x7B605E; [0xA8CCDC]=0x00AAEE78 -> ptd 0x00B93694 -> secondary name; vtable extent 47 code-pointer entries. NUMERATOR/DENOMINATOR: 1 block identity / 1 walk (calibrated). PRIMARY_EVIDENCE: 01_RAW/SF30_RTTI_RAW.txt. INDEPENDENT_VALIDATION: QC own walker + PE-MASTER own dword/name reads (all three chains). ASSUMPTION_LINEAGE: MSVC RTTI layout (verified from the calibration known-answer, not assumed). ALTERNATIVE_EXPLANATIONS: a second vtable store overwriting the block before storage — excluded at the receipt level (the block is stored immediately after the ctor returns; P1 chain byte-locked). FALSIFIER: a different name at the walk, or the ctor not being the value source. MISSING_LINKS: the semantic ROLE of the NiNode (model bridge) NOT TESTED — by contract; SF30_LINK_TYPE_IDENTIFIED is the maximum label and it is what is claimed.
3. CL-SF30-CENSUS — the census artifact is enumeration-complete over .text: 3643 candidates = 2 PROVEN + 618 POSSIBLE_ALIAS + 3023 REJECTED_ALIAS + 0 UNRESOLVED; every candidate has a 4-class row; coverage classes (i)-(iv) all scanned (combined-offset writes structurally impossible because the SF slot holds a POINTER; bulk-init measured to target [esp+0x18] and [ebp+0x4C] — neither covers +0x30). STATUS: CONFIRMED in the enumeration-completeness sense; resolution-completeness of all 3643 receivers is NOT claimed (POSSIBLE_ALIAS is the contract-designed honest residue; the human contract's FINAL_STATUS taxonomy adjudicated: A stands under the contract's own definition — QC sections 5-6 concur; the stricter "all receivers resolved" reading would empty the contract's own POSSIBLE_ALIAS class of meaning and is disclosed at every level of the package). FALSIFIER: any write-disp0x30 candidate outside the 3643 (none found by two independent implementations under a superset rule). INDEPENDENT_VALIDATION: QC exact sweep reproduction + PE-MASTER own recount/distribution.
4. CL-SF30-NEXTSEAM — NiNode vtable slot 17 dword [0xA8CD38] = 0x007B5390; NEXT_SEAM = SLOT17. STATUS: CONFIRMED (VALUE recorded; NOT decoded — scope held; PE-MASTER's own byte read). FALSIFIER: dword mismatch (none).
5. CL-ADMIN — the previous run's committed state: 27 package files / 26 manifest rows + manifest self / 28 commit paths including AUDIT_ENTRYPOINT.md. STATUS: CONFIRMED (PE-MASTER own diff-tree count of 1a490ee + QC own count). The old "26/25" figures are superseded and appear only inside correction statements.
GATE_PREDICATES  = G0-SOURCE-BASE PASS (fail-closed identity+base; the dirty-path set was amended pre-run with the disclosed expected-untracked-package note — necessary because the executor is forbidden git mutations while required to create the package; false-fail: none plausible). G1-POSITIVE-CONTROL PASS (bytes (a)(b)(c) + ECX-unmodified + receiver chain — necessary for RUN_INVALID discipline; re-measured at gate time AND independently by PE-MASTER; a failure would have made the run RUN_INVALID — the gate can demonstrably fail). G2-CENSUS-COMPLETENESS PASS (enumeration rule + range + denominator documented; 3643/3643 classified rows; counts recomputed from rows == raw == state json; coverage (i)-(iv) present in raw; the 15 UNATTRIBUTED rows fall to the conservative branch — asserted by classification-rule analysis). G3-PROVENANCE PASS (both writers carry creation/receipt-level hop tables with VA+byte evidence, both RESOLVED; no UNRESOLVED edge needed; every hop byte-verified by QC + PE-MASTER). G4-TYPE-IDENTITY PASS (calibration-first fail-closed walker; the expected-string now carries the corrected TD+0x08 label after amendment R1 — the original label "TD+0x0C" was a PE-MASTER contract defect, AMEND_LOG_R1.md 8.1; re-walked at gate time and independently by QC + PE-MASTER). G5-SCOPE-HELD PASS (zero forbidden RESULT claims; zero slot-17/0x437F70/0x82B5A0 analyses; zero census rows inside forbidden ranges; NOTE: the script census scope post-amendment = the EXECUTOR set — AMEND_LOG_R1.md 8.2 with the measured 7 trip-lines rationale; the QC files are covered by QC_AUDIT_R1.md C7 and by this review's own full-package line-by-line census: ALL occurrences of the forbidden labels in the whole package are machinery strings or sanctioned negative statements, ZERO claim-context; the detector's substring-permissiveness (QC P2-3) is a NEXT-GATE-TEMPLATE fix, deliberately not amended in-run).
CODE_FINDINGS    = NONE remaining after amendment R1. Found and FIXED (all in the render/documentation layer, no census semantics touched; deterministic regeneration with AMEND_LOG SHA-pairs; SF30_WRITER_CENSUS.csv byte-identical; counts unchanged 3643/2/618/3023/0; gates CSV byte-identical): the census.py hit-VA render bug (P3-6), the 4 receiver-site VA citations (P2-1), the TD+0x0C label in census.py/finalize.py/SF30_RTTI_RAW.txt (P2-2, origin = PE-MASTER's contract), the finalize G4 expected-string (2a). The finalize.py executor-set filter (2b) is a disclosed input-universe restoration, not a predicate change (AMEND_LOG_R1.md 8.2).
EVIDENCE_FINDINGS = NONE remaining after amendment R1 (manifest 17/17 re-hash OK; SCRIPT_SHA256 4/4; QC inventory untouched — all 12 qc_probe files + QC_AUDIT_R1.md byte-equal to QC section 9's pre-amendment hashes; no .pre files created; the UTF-8 encoding switch of the 4 regenerated evidence files documented, AMEND_LOG_R1.md 8.3).
CANON_CONFLICTS  = NONE. Cross-confirmations: (1) the accepted PE_935_POSITION_CONSTRUCTION_CORRECTIONS_R1_20260913 REPORT section 4.1 had ALREADY established the SF-ctor association new(0x118) -> FUN_007B6000 -> [SF+0x30] with refcount@+4, characterized as "scene object" with NO class name — this run CONFIRMS that chain byte-level and adds the class identity; the REPORT now credits it (amendment P3-10); (2) prior LOCAL-ONLY instrumentation naming (GA3_CTOR_NiNode_007B6000 ghidra dumps; "NiNode 0x00A8CCF4" in a prior qc probe list) matches the now byte-confirmed identity — analyst-label provenance is superseded by a published, calibrated RTTI walk; no prior PUBLISHED claim contradicted; (3) the SLOT_CENSUS slot-3 chain (0x50A05B read; [vtable+0x44] dispatch) re-derived identically as this run's positive control. NAMING NOTE: the RTTI string .?AVNiNode@@ is the ENGINE C++ class (NetImmerse runtime); it is NOT a NIF-file block-type identity claim — no conflation with the NIF-format canon.
RETRACTIONS      = NONE required (no prior claim contradicted; nothing retracted cited as standing; the superseded "26/25" administrative figures were already corrected by the human's contract and are carried correctly).
PERSISTENCE_CHECK = Pre-publication: package on disk (31 files), untracked; HEAD == BASE; zero tracked-file modifications by executor/QC/amendment (git diff empty). Publication (this commit): path-limited to the package + AUDIT_ENTRYPOINT.md only; staged-path census verified by the persistence worker before commit; pushed; HEAD == origin/master == ls-remote verified. The publication manifest covers the full published set (the executor finalize manifest state preserved in AMEND_LOG_R1.md section 6; supersession documented). ENTRYPOINT row +1/-0 (the previous top row PE_935_SCENEFEEDER_SLOT_CENSUS_R1_20260914 intact — L07 row-survival control).
APPLICATION_READY= N/A — this run makes no live-documentation application proposal; the only doc target (AUDIT_ENTRYPOINT LATEST RUNS row) is executed by the persistence worker with exact text and +1/-0 row survival.
CHECKPOINT_DELTA = NONE (PE_CURRENT_CHECKPOINT.md untouched — the FOUNDATION-era bootstrap file is not per-run updated; the run state is carried by AUDIT_ENTRYPOINT.md per convention).
NEXT_EXPERIMENT  = DESIGNED, NOT DISPATCHED — the human's contract authorizes the next run SEPARATELY (after the Desktop post-audit). The one P0 for the next authorized run: decode NiNode vtable slot 17 (0x007B5390) — the exact virtual target invoked by FUN_0050A050's primary path on the SF+0x30 link — DOWN TO ITS FIRST CALL ONLY, STATIC-ONLY, under fresh scope guards (maximum label CANDIDATE_MODEL_BRIDGE per the prior accepted run's pre-registration; TRANSFORM_TO_MODEL stays NOT_DEMONSTRATED unless the slot-17 chain closes it byte-level). Template fixes from this run to bake into that contract: state expected offsets ONLY as fail-closed pins to re-derive (correct MSVC label: name @ TD+0x08); harden the forbidden-label gate to an explicit whitelist of sanctioned negative statements + record the file/line census in the gates row; state the G5 census scope explicitly. Gate design sketch: inputs = EXE pin (SHA256 E7785430...F31 / 8015872) re-verified fail-closed + vtable 0x00A8CCF4 + slot-17 value 0x007B5390 (both re-derived in-run, never trusted); PASS = slot-17 body decoded to its FIRST call with VA+byte evidence + the call target's class identified (RTTI walk if a vtable store exists) + the receiver chain proven; non-pass classes = UNRESOLVED_EDGE / BODY_NOT_REACHABLE_STATICALLY / SCOPE_BREACH; hard stops = source/base drift, any runtime execution attempt, extension beyond the first call, any model-bridge claim beyond CANDIDATE_MODEL_BRIDGE.
ORDERED_WORK     = This persistence step (pe-master-auditor): persist this review VERBATIM; regenerate the publication manifest over the full published set; add the ENTRYPOINT row (+1/-0); path-limited commit + push + remote verification; append the L22/L23 trap-library entries + the NiNode ground-truth pin to the pe-master-audit skill (append-only, outside the repo, SHA before/after recorded); report the delivery notice. NO other writes; no canonical state changes; no milestone action.
HANDOFF_BLOCK    = AUDIT_OUTPUT_ROOT (NEXT authorized run): docs/audits/<NEXT_RUN_ID>/ (fresh, verified absent before creation) / FINAL_REPORT_PATH (this run): docs/audits/PE_935_SCENEFEEDER_LINK30_IDENTITY_R1_20260914/06_REPORT/REPORT.md / PRIMARY_EVIDENCE_PATHS (this run): 02_ANALYSIS/SF30_WRITER_CENSUS.csv; 02_ANALYSIS/SF30_PROVENANCE.md; 01_RAW/SF30_WRITER_RAW.txt; 01_RAW/SF30_RTTI_RAW.txt; 01_RAW/POSITIVE_CONTROL_0050A050.txt; 06_REPORT/AMEND_LOG_R1.md; 06_REPORT/QC_AUDIT_R1.md; 06_REPORT/PE_MASTER_REVIEW.md; 06_REPORT/MANIFEST_SHA256.csv / RUN_STATUS: COMPLETE (FINAL_STATUS A — IDENTIFIED; amendment R1 applied; publication this commit) / HARD_STOP_REASON: NONE — the contract's HARD STOP applies: after this run's identification + return block, the next run is authorized separately by the human after the Desktop post-audit.

## Self-adversarial pass record (sections 11.7 / 14.1 / 14.7 / 15.17)

- PHASE 3 checks A-M run; PHASE 4 final reconciliation run; 14.7 items 01-22 all answered (key ones: no invented denominator — the 618 residue is kept as residue, never rounded to zero; the "exactly 2 dword occurrences" claim was PE-MASTER's own STRONGER whole-file re-derivation; independence lineage named including the shared-capstone caveat; no structural->semantic promotion — the NiNode identity is RTTI-level, its ROLE stays unidentified; retraction-opposite avoided; every hard gate audited necessary+sufficient; MSVC TypeDescriptor layout verified from bytes; blast radius by dependency edges only; RUN_CLASS canonical (LOAD_BEARING); QC depth fresh-context per class; era labels preserved; no cross-build promotion; no self-oracle; availability never called correctness; exact denominators; no percentages; the NEXT_EXPERIMENT design checked for hypothesis-leakage — it measures what slot 17 does with a label CEILING, not an embedded expectation).
- SELF_FINDINGS_DETECTED = 3; SELF_FINDINGS_RESOLVED_IN_FINAL_DRAFT = 3: (F-M1) QC's P2-2 location list slightly over-inclusive re REPORT.md — resolved: recorded in STATUS_ALGEBRA, no edit to QC's own record; (F-M2) the package exceeds the human's "~10-15 files" target (31 + QC additions) — resolved: disclosed here, justified by the contract-required QC artifacts + generator canon + the contract's own QC-exception clause; (F-M3) the "TD+0x0C" contract-label defect was PE-MASTER's own formalization error — resolved: self-attributed, corrected by amendment R1, lesson L22 appended to the trap library. (Plus tool-side traps: three PowerShell counter-check defects — case-insensitive variable collision, alias shadowing, execution policy — all resolved BEFORE any counter-check evidence was produced; lesson L23 appended.)

— PE-MASTER, MASTER_AUDIT of PE_935_SCENEFEEDER_LINK30_IDENTITY_R1_20260914, 2026-09-14. Verdict: MASTER_ACCEPTED (advisory; PROVISIONAL_UNTIL_QUALIFIED; CANONICAL_GATE_EFFECT=NONE).
