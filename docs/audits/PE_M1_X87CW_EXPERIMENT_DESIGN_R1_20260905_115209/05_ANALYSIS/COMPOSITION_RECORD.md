# COMPOSITION RECORD — which canon facts compose this design + their evidence pointers

**RUN_ID:** PE_M1_X87CW_EXPERIMENT_DESIGN_R1_20260905_115209
**RULE:** this DESIGN-ONLY run composes the EXISTING locked canon — every load-bearing statement in `EXPERIMENT_DESIGN.md` maps to a canon fact listed below, or to a bounded EXECUTION-phase check (explicitly marked), or to an operational-discipline rule (the profile/skill sources). ZERO facts were invented; ZERO new static analysis was performed.

## THE MAP (design element → canon fact → pointer)

| # | Design element (where used) | Canon fact composed | Pointer (file, SHA256, field/line) |
|---|---|---|---|
| C1 | W1.1 the FDIV site: VA 0x0098CE5A, bytes `DC 35 A8 D7 A7 00`, consuming 32767.0 f64 @0x00A7D7A8 (.rdata, file-backed, bytes `00 00 00 00 C0 FF DF 40`); role "the rand01 divisor" | CONSTANT_ADDRESS_LOCK `three_qword_constants[0]` | CONSTANT_ADDRESS_LOCK.json, 6F4A9A6E..., `three_qword_constants[0]` (extract X1.1) |
| C2 | W1.2 the FLD site: VA 0x0095B2BC, bytes `DD 05 58 C7 A8 00`, consuming 65535.0 f64 @0x00A8C758 (.rdata, bytes `00 00 00 00 E0 FF EF 40`); role "the u16 position divisor" | CONSTANT_ADDRESS_LOCK `three_qword_constants[1]` | CONSTANT_ADDRESS_LOCK.json, 6F4A9A6E..., `three_qword_constants[1]` (extract X1.2) |
| C3 | W1.3/W1.4 the chain context: the third byte-locked site (FMUL @0x0095B347, _DAT_00a980d0, the node-scale multiplier) | CONSTANT_ADDRESS_LOCK `three_qword_constants[2]` | CONSTANT_ADDRESS_LOCK.json, 6F4A9A6E..., `three_qword_constants[2]` (extract X1.3) |
| C4 | W1.3 FUN_0098fe00 = the procedural grid path (INTEGER-ONLY, subdivision 0..4 → steps 1/2/4/8/16, 24B cells, density = *(cell+8)>>3); FUN_00990810 = the stored {u16,u16,u32} records | V4 row 10 `knowledge` | M1_GATE_DELIVERABLE_MATRIX_V4.json, 003056AC..., row 10 `knowledge` (extract X3.1); MD EC04FC47..., ROW 10 @ line 134 |
| C5 | W1.3/W1.4 FUN_0095b180 = the spawn path: nodeX/Y = f32(u16/65535.0) at node+0x5C/0x60 (the FLD site executes here); nodeScale at node+0x68 (the FMUL site); the u32 model bind at node+0x64; rotation IDENTITY; six FSTP-DWORD f32 points; 76/76 bit-exact | V4 row 10 `knowledge` | M1_GATE_DELIVERABLE_MATRIX_V4.json, 003056AC..., row 10 `knowledge` (extract X3.1); MD @ line 134-136 |
| C6 | W1.1 the RNG path: seed = ((p4*16+p5)*16+p1+p2+p3)*0x5CC7+0x6D7, MSVC rand() LCG, rand01 = f32(r/32767.0) with the FSTP DWORD @0x0098CE60; the sampler FUN_0095ac30 FSTP @0x0095ACF0; FUN_0098ce30 named (the dead-code FADD idiom); p2 = the VIEW BAND; NO global seed, NO server RNG | V4 row 11 `knowledge` + `evidence` (iter032_re_dec_0098cdf0_rng_seed.c, iter032_re_dec_0098ce30_rng_next.c) | M1_GATE_DELIVERABLE_MATRIX_V4.json, 003056AC..., row 11 `knowledge`+`evidence` (extracts X3.2/X3.3); MD ROW 11 @ line 146 |
| C7 | W1/W3.6 the byte-locked operand statements (FDIV/FLD/FMUL site-address bindings) | V4 row 11 `implementation` | M1_GATE_DELIVERABLE_MATRIX_V4.json, 003056AC..., row 11 `implementation` (extract X3.3) |
| C8 | W2/W5 the P0 condition: "the engine-parity claim is CONDITIONAL on PC in {53,64} + RC=nearest-even (the documented Win32 default CW 0x027F; the ACTUAL client CW is UNMEASURED — the falsifier = a runtime capture)" | V4 row 11 `limitations` | M1_GATE_DELIVERABLE_MATRIX_V4.json, 003056AC..., row 11 `limitations` (extract X3.4); MD @ line 155 |
| C9 | W5.1/W5.2 the numbers: PC=24 breaks 14,104/229,376 (6.15%) of the REAL lerp domain + 103,073/1,245,184 of the labeled synthetic domain (double-confirmed) | V4 row 11 `limitations` + `known_open_list_v4[6]` + `honest_limits_binding[1]` | M1_GATE_DELIVERABLE_MATRIX_V4.json, 003056AC..., (extracts X3.4/X3.5/X3.6); MD @ lines 155, 322, 334 |
| C10 | W0/W2.3 the open item: "the actual x87 control word at chain-execution time ... a runtime capture is the falsifier" | V4 `known_open_list_v4` item 7 | M1_GATE_DELIVERABLE_MATRIX_V4.json, 003056AC..., `known_open_list_v4[6]` (extract X3.5); MD @ line 322 |
| C11 | W2/W6 the PC-mode table: PC=24 single ("the intermediate rounds to a 24-bit mantissa"), PC=53 double ("matches the DOCUMENTED Win32 process-default CW 0x027F (PC=10b double precision)"), PC=64 extended ("the FINIT/x87-reset default PC=11b") — the decode labels W3/W5 use | oracle_battery `pcrc_conditional_model.pc_modes_compared` | oracle_battery.json, B04A3175..., `pcrc_conditional_model.pc_modes_compared` (extract X4.1) |
| C12 | W2.3/W5 the domain insensitivities: rand01 (32,768) + position (65,536) domains — all three PC modes agree (0 mismatches); the lerp/scale domains are the PC-sensitive ones (measured in the repair run, work item 2) | oracle_battery `pcrc_conditional_model.rand01_domain_r_32767` + `.position_domain_u_65535` + `.conditional_model` | oracle_battery.json, B04A3175..., (extract X4.2) |
| C13 | W2.2/W2.3 the FLDCW chain-level fact: "the frozen iter035 operand census lists every FPU instruction of the 17 audited foliage-chain functions (51 instructions); NONE is FLDCW/FLDENV/FRSTOR — the audited chains do not modify the control word themselves" | oracle_battery `text_cw_bytepair_scan.chain_level_static_fact` | oracle_battery.json, B04A3175..., (extract X4.3) |
| C14 | W2.3 the census caveat: the 910 raw d9/dd byte-pair count is "a raw byte-pair sensitivity count, NOT an instruction census" | oracle_battery `text_cw_bytepair_scan.caveat` + `raw_d9_dd_cw_operand_pairs_in_text` | oracle_battery.json, B04A3175..., (extract X4.3) |
| C15 | W2.3/W6 the unmeasured mechanism candidate: "thread default + possible D3D8 FPU-mode changes if the device was created without D3DCREATE_FPU_PRESERVE" + the RC assumption statement ("the PLATFORM DOCUMENTED DEFAULT, NOT a measurement of the original client") | oracle_battery `unmeasured` + `rc_assumption` | oracle_battery.json, B04A3175..., (extract X4.4) |
| C16 | W5.2 the measured sensitivities: lerp_scale_real — 229,376 checks, engine-vs-JS 0 mismatches, lerp_pc24_mismatches = 14,104; rand01/positions — engine-vs-pc24 0 mismatches; the 7 REAL original-VCL pairs; scale_pc24_mismatches = 0 | domain_reproof `domains.lerp_scale_real` + `.rand01_r_32767` + `.positions_u16_65535` + `sets` | domain_reproof.json, E654D2EF..., (extract X5) |
| C17 | W5.1 the synthetic-domain double confirmation: 103,073/1,245,184 CONFIRMED by the run-side re-measurement | V4 row 11 `validation` + `this_run_evidence.pc24_synthetic_measurement` | M1_GATE_DELIVERABLE_MATRIX_V4.json, 003056AC..., row 11 (extracts X3.2-adjacent); pc24_synthetic_measurement.json 01B96D25... (repo mirror docs\audits\PE_M1_GATE_V4_CORRECTION_R2_20260905_101327\01_RAW\) — re-hashed this run, MATCH |
| C18 | W3.1/W3.2 the binary identity: Entropia.exe, PCG_9_3_5, SHA256 E7785430..., 8,015,872 B (read-only identity pin; never launched; never committed) + image_base 0x00400000 + .text/.rdata raw_offset==rva (the VA→file mapping of the sites) | prompt §2 + CONSTANT_ADDRESS_LOCK `binary` + PE_SECTION_MAP | CONSTANT_ADDRESS_LOCK.json 6F4A9A6E...; PE_SECTION_MAP.json C5688A53... (extracts X2/X6); offline_rechecks.json C80E65D6... `inputs.exe` |
| C19 | W3/W4 the operational discipline: §14 rules 1-15 (control-plane ≠ target; independent exit proof; the separated classes; NOT_OBSERVED ≠ REJECTED; bounded capture; ACTIVE_ORPHANED; the derivation chain) + §16.4 forecast labels + §16.1 split verdicts + §16.7 RUN_ID reuse | pe-master-auditor profile §14/§16 | D:\Eudoria_Reconstruction\.opencode\agents\pe-master-auditor.md, §14 @ line 250, §16 @ line 318 (extract X7) |
| C20 | W3 the x32dbg tool recipe: the version pin (0.0.2.5, SHA256 822028F0...), the portable-copy discipline, the spawn-to-system-breakpoint recipe, `bp <VA>`, BPs-before-run, the hash-verified-sandbox anti-pattern, the measured GUI/automation blocker, the session-artifact pattern (SESSION_LOG + screenshot + the honest not-achieved notes) | pe-x32dbg-runtime skill | D:\TESTAI\.opencode\skills\pe-x32dbg-runtime\SKILL.md (extract X7); the tool pin re-hashed this run at D:\x64dbg\release\x32\x32dbg.exe — MATCH |
| C21 | W3.1 the sandbox environment layout (the runtime-prerequisite mirror: the sibling DLL set + the Data\ tree of the install) | the observed pcg_install directory layout (environment fact, not a binary claim) | D:\Eudoria_Reconstruction\pcg_install\ (directory listing observed this run; the EXE pin C18) |
| C22 | W6 the forecast bases: the documented-default 0x027F (MOST_LIKELY), the FINIT default PC=64 (PLAUSIBLE via C11), the D3D8-without-FPU_PRESERVE mechanism (LOW_PROBABILITY via C15) — all §16.4-labeled, none pre-committed | composed from C11+C13+C15 | oracle_battery.json B04A3175... (extracts X4.1/X4.3/X4.4) |

## EXECUTION-PHASE BOUNDED CHECKS (NOT static claims — each is a runtime verification or a measured observation, pre-declared)

| # | The check | Why it is execution-phase (the canon does NOT answer it statically) |
|---|---|---|
| E1 | The module-base verification at spawn (== 0x00400000 → verbatim VAs; else delta-shift) | the canon maps the image (C18) but holds no claim about runtime relocation; the check makes the procedure correct either way WITHOUT a static ASLR claim |
| E2 | The byte pre-verify at the bp sites (`DC 35 A8 D7 A7 00` / `DD 05 58 C7 A8 00`) | the bytes are canon (C1/C2); the LIVE memory check is the execution-phase confirmation that the loaded image matches the pinned build |
| E3 | The site-local CW read + the N=10-hit series + the cross-site agreement | the canon says the ACTUAL CW is UNMEASURED (C10) — this IS the measurement |
| E4 | The init-CW vs site-CW comparison (the W2.3 disambiguation) | the canon names the candidate mechanism (C15) but not the fact; the comparison is the bounded observation that resolves it |
| E5 | The FLDCW-presence question between init and the sites | the canon answers the CHAIN level only (C13); the binary-wide question is explicitly NOT a census (C14); the N-hit stability + the init-vs-site equality are the bounded runtime evidence — a binary-wide instruction-boundary census would be NEW static analysis and is therefore OUT of scope for the execution run too |

## GAP CHECK (the prompt's CANON_GAP rule)

Every design need was checked against the canon before composing:

1. The target addresses + bytes + roles → covered (C1-C7).
2. The chain functions' roles + which site belongs to which path → covered by the V4 rows 10/11 role/knowledge fields, stated EXACTLY as the record states them (C4-C7). The canon does not state a caller census — the design needs none (the breakpoints are at the canon VAs; W1.3 records this boundary honestly).
3. The CW read points + the disambiguation + the FLDCW-presence question → the canon covers the chain level (C13) and names the unmeasured mechanism (C15); the binary-wide census is honestly bounded OUT (E5) instead of invented. **COVERED — no gap.**
4. The PC-mode decode table + the PASS/FAIL numbers → covered (C11, C16, C17).
5. The tool recipe + the operational taxonomy → covered (C19, C20).
6. The binary identity + the VA→file mapping → covered (C18).

**CANON_GAPS_FOUND: NONE.** RUN_STATUS per this check = DESIGN_COMPLETE (no gap-driven STOP; no invented fact).

If the PE-MASTER review identifies a canon fact this design needs but the canon lacks, the expected disposition is: CANON_GAP reported + a separate-run static proposal — NOT an in-run static extension (the prompt §5 NON-PASS branch, honored by construction above).
