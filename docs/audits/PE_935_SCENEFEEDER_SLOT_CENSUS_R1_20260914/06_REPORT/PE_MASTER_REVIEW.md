# PE_MASTER_REVIEW - PE_935_SCENEFEEDER_SLOT_CENSUS_R1_20260914

**VERDICT: MASTER_ACCEPTED (advisory)** - PROVISIONAL_UNTIL_QUALIFIED; CANONICAL_GATE_EFFECT = NONE. This run does not close or promote any milestone and does NOT change TRANSFORM_TO_MODEL = NOT_DEMONSTRATED.

AUDITED: the complete local package (final state: 26 files) against the human's bounded run order (RUN_CONTRACT.md) on BASE_SHA 6465019298e66856a2fe9fa9750c047d057c97dd, including the INTERNAL_QC round and the PE-MASTER-ordered amendment round.

## PE-MASTER independent verification (own byte reads from the physical EXE)
- G1: EXE SHA256 E7785430... (8,015,872 B) re-measured - match.
- G2: vtable 0x00A7D458 read directly from .rdata: six dwords = 0x0050A460/0x005090A0/0x005090B0/0x0050A050/0x005090C0/0x00509580 (the six contract functions ARE the complete vtable; the 7th dword = 0x53565064 "dPVS" boundary); RTTI walk own: [0x00A7D454] -> COL 0x00AA12B8 -> TD 0x00B78834 -> .?AVSceneFeederObject@@ - CONFIRMED.
- Slots re-decoded by PE-MASTER: slot 0 = scalar deleting dtor (CALL 0x0050A240 dtor body; operator delete CALL 0x0095D42A; TEST [ESP+8],1 JE - the deleting-dtor idiom; zero window fields); slot 1 = 8D 41 34 C3 (LEA EAX,[ECX+0x34]; RET - position-ADDRESS getter, no value read); slot 2 = 8D 41 74 C3 (+0x74 out of window); slot 4 = 8D 81 80 00 00 00 C3 (+0x80 out of window); slot 5 = reads [ECX+0x18], CALL 0x004150F0 and CALL 0x008B71D0 - no window fields involved.
- Slot 3 (FUN_0050A050) fully re-decoded by PE-MASTER and CONFIRMED byte-exact: arg2!=NULL primary path: MOV ECX,[ESI+0x30] @0x0050A05B (the +0x30 LINK read), MOV EDX,[ECX] (link vtable), PUSH EAX (query flag), MOV EAX,[EDX+0x44] (virtual slot 17), CALL EAX @0x0050A064, TEST/JE fallback, ADD EAX,0x90 -> CALL 0x00437F70 -> MOV ECX,EAX -> CALL 0x0082B5A0 (fills arg1); arg2==NULL fallback: MOV EDX,[ESI]; MOV EAX,[EDX+4] (vtable slot 1 = the proven getter); MOV ECX,ESI; CALL EAX @0x0050A08E; reads X/Y/Z at 0x0050A090/0x0050A098/0x0050A09E and copies the triple to the caller's arg1 buffer at 0x0050A096/0x0050A09B/0x0050A0A1. NO position VALUE enters ANY call. CONFIRMED: classification MIXED (link read + position read; position sub-behavior READ_LOCAL); position_to_call = NO for every slot.
- G3: CSV 6/6 rows x 19 contract columns; vocabulary closed - CONFIRMED.
- G4: raw instruction evidence present for the position-reading slot and the address getter; negative slots carry full-listing evidence - CONFIRMED.
- G5: vacuous PASS - no slot passes position values into a call; zero MODEL/NiAVObject/ArkModelResourceInstanceRef/TRANSFORM_TO_MODEL/CANDIDATE_MODEL_BRIDGE claims - CONFIRMED.
- Positive control: FUN_005094C0 re-measured byte-exact - PASS.
- Package integrity final: 26 files; MANIFEST_SHA256.csv 25/25 re-hashed OK (path,bytes,sha256), no self-row, zero duplicates; SCRIPT_SHA256 4/4 (script,bytes,sha256,purpose); no __pycache__; QC_AUDIT_R1.md integrity preserved across the executor amendment (sha 8A8CB47F...).

## Findings (adjudicated through the full chain: executor -> fresh INTERNAL_QC -> PE-MASTER -> executor amendment -> PE-MASTER re-verification)
- [P2, ACCEPTED_FINDING, RESOLVED-BY-AMENDMENT] FALSE TOOLCHAIN VERSION LABEL: two raw-evidence headers and three script literals claimed "capstone 5.0.9" while the measured toolchain is capstone 5.0.7 (Python 3.12.7, D:\Eudoria_Reconstruction\10_Scripts\python_env\python.exe), contradicting the package's own identity files (RUN_CONTRACT/SOURCE_IDENTITIES/SCRIPT_SHA256 all 5.0.7). Found by the fresh INTERNAL_QC (44/44 independent byte checks; the defect was missed by both the executor SELF_CHECK and the PE-MASTER pre-QC audit - the fresh-context QC layer worked exactly as designed and correctly STOPPED publication per the binary defect rule). Science impact: ZERO (every load-bearing byte was independently verified without capstone by both the QC and the PE-MASTER). PE-MASTER adjudication: publication of a false provenance label = forbidden; WAIVER DENIED; executor amendment ordered and verified: labels made dynamic (capstone.__version__), raw files regenerated with byte-exact .pre provenance, instruction listings byte-identical except the version line (determinism proven; JSON sidecars byte-identical), SCRIPT_SHA256 + manifest refreshed, AMEND_LOG_R1.md written; zero live false labels remain (the pre-fix strings survive only as documented quotes in the QC record, the AMEND_LOG and the .pre snapshots - by design).
- No other findings. The run's layer separation, negative-result honesty, one-hop stop discipline and closed classification vocabulary were all respected.

## Status algebra (kept separate)
- CLAIM_KNOWLEDGE_STATUS: the six contract functions = the complete SceneFeederObject vtable 0x00A7D458 (CONFIRMED); slot 3 = the position reader (CONFIRMED, static decode level); slot 1 = the position-address getter (CONFIRMED); no position-to-call exists (CONFIRMED negative within this vtable); SF+0x30 link object class UNKNOWN (the run's declared open edge); TRANSFORM_TO_SCENEFEEDER = PROVEN (prior run, unchanged); TRANSFORM_TO_MODEL = NOT_DEMONSTRATED (unchanged); 4508/296445 untouched.
- EXECUTABLE_GATE_STATE: G1-G5 PASS; positive control PASS.
- HUMAN_REVIEW_STATE: PENDING (advisory pre-qualification; the human ordered an independent Desktop post-audit of this package).
- RUNTIME_SOURCE_CLASS: STATIC-ONLY decode of the physical EXE; engine-client execution ABSENT.

## FINAL_STATUS: B - a slot reads the position but does not send it into a call. NEXT_SEAM = NONE. The one next test (for a FUTURE authorized run, not this one): SF+0x30 link identity - writer-scan of [SF+0x30], RTTI of the link class, and the decode of its vtable slot 17 (the virtual target of the 0x0050A064 call) down to the first call; maximum outcome label CANDIDATE_MODEL_BRIDGE.

## Scope honesty
PE-MASTER personally byte-verified: the vtable (6 dwords + boundary + RTTI chain), all six slot bodies (full manual decode including all E8/E9 call targets re-resolved), the positive control, the census CSV schema/content, the manifest and SCRIPT_SHA256 (re-hash), the amendment diffs vs .pre (exactly one version line per regenerated file), the version-label census (zero live false labels), the package census. NOT_CHECKED (beyond the run's declared boundaries): callee bodies beyond one-hop (0x0050A240 dtor body, 0x00437F70, 0x0082B5A0, 0x004150F0, 0x008B71D0, the dynamic link->vtable[+0x44] target), the SF+0x30 link class identity, slot callers, runtime overrides. These are the run's declared NOT_CHECKED - no new gaps were found inside the contracted scope.
