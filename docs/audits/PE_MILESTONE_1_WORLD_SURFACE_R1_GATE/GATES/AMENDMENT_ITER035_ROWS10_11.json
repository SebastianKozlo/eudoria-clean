{
  "iter": "035 (ledger ITER_035) — THE MATRIX ROWS 10/11 RE-JUDGMENT after the FLOAT64 operand lock (the human audit's ENTRY #10 demotions, corrected verdicts; input to the master auditor's gate re-audit)",
  "basis": {
    "rejection_recorded": "ledger ENTRY #10: the human rejected the M1 gate PASS; rows 10/11 were demoted CONFIRMED -> PARTIAL/REJECTED-as-implemented; this iteration's P0 locked the operands from the binary and revalidated",
    "new_evidence": [
      {"file": "iter035_operand_table.json", "what": "the byte-ex FLOAT64 operand lock: the three constants + the full operand-width census (17 functions, 51 FPU instructions) + the six f32 rounding points + the exhaustive bit-exactness proofs"},
      {"file": "iter035_rng_crosscheck.json", "what": "the ANTI-CIRCULAR revalidation: 76/76 instances BIT-EXACT (every f32 field, every u32 state, every position) vs a reference deriving its constants from the binary extraction and its records from the ORIGINAL VegetationClimates.bnt; the built-in vector 9719 PASS; instance 0 IS the vector case"},
      {"file": "iter035_foliage_render_hashes.json", "what": "the fresh deterministic render (8770AAA0..., 3/3 loads) + the superseded old hash recorded + the delta root-caused + the off-hash equality proving the terrain path untouched"},
      {"file": "iter035_old_reference_negative.json", "what": "the demonstrative negative: the OLD shared-assumption reference now FAILS 76/76 against the corrected page (the correction is material in every case, exactly as the human computed)"},
      {"file": "iter035_pattern_findings.json", "what": "the pattern re-check: 71 evidence-cited constants width-locked; 27 QWORD f64 constants; the 7 f32-widened-to-f64 constants with misleading low dwords (report-only follow-ups)"}
    ]
  },
  "row_10_FOLIAGE_DISTRIBUTION": {
    "old_verdict_REJECTED": "CONFIRMED (mechanism) + IMPLEMENTED with era-bounded [P-CELLSTREAM] stand-in — the iter033/034 state; the 'scale = |rand*2.0| MEASURED' knowledge claim was WRONG (2.0f was the LOW DWORD of the f64 1/12800 constant _DAT_00a980d0); the 'pos = u16/K' knowledge with K=2.0 was WRONG (the divisor is 65535.0 f64 and produces the [0,1] NODE-LOCAL fraction, not a world scale)",
    "corrected_verdict": "PARTIAL -> CORRECTED-TO-CONFIRMED (mechanism): the MECHANISM is engine-confirmed (FUN_0098fe00 integer-only grid; FUN_00990810 {u16,u16,u32} records; FUN_0095b180 spawn); the implementation arithmetic is NOW BYTE-LOCKED (nodeX/Y = f32(u16/65535.0); nodeScale = f32(|lerp*0.00007812499825377017|); the f32 rounding at the binary's FSTP points) and revalidated 76/76 BIT-EXACT vs the binary-derived reference; the CELL CONTENT remains the labeled RECONSTRUCTION stand-in",
    "corrected_knowledge": "FUN_0098fe00 procedural grid (subdivision 0..4 -> steps 1/2/4/8/16; INTEGER-ONLY per the iter035 census; 24B cells; density = *(cell+8)>>3); FUN_00990810 stored cell records {u16 x, u16 y, u32 id}; FUN_0095b180 spawn loop (node+0x5C/0x60 = f32(u16/65535.0) = the [0,1] NODE-LOCAL position; node+0x68 = f32(|value * 1/12800|) scale; node+0x64 = the u32 model bind; rotation IDENTITY = the RE-faithful absence); the [0,1]-node->world mapping lives in the visualizer path (NOT decompiled — the page's window calibration is a labeled CURRENT_RUNTIME_CALIBRATION)",
    "reconstruction_only_labels": {
      "placementHash_and_round_density": "RECONSTRUCTION-ONLY: the per-cell record CONTENT (positions via the placement hash + counts = max(0, round(col1 density))) is the documented deterministic stand-in for the historical cell byte stream — NEVER historical truth (the stream origin remains open, iter032 bound 3)",
      "cell_stream_origin": "OPEN: the historical cell byte-stream origin (local file vs server content) is NOT closed; the record FORMAT + the spawn arithmetic are the CONFIRMED parts",
      "P-CLIMATE": "RECONSTRUCTION-ONLY: the climate index choice (0.vcl on the page) is a documented page choice; the per-location climate input (the shared-selector hypothesis) is PLAUSIBLE-UNVERIFIED"
    },
    "historical_fidelity": "MECHANISM engine-confirmed + the arithmetic BYTE-LOCKED (this iteration); CELL CONTENT = RECONSTRUCTION-ONLY stand-in (labeled, open)",
    "evidence_status": "CONFIRMED (mechanism + arithmetic, iter035 byte-locked) / [P-CELLSTREAM]+[P-CLIMATE]+[P-WINDOW] RECONSTRUCTION-ONLY (content/selection/window)"
  },
  "row_11_FOLIAGE_SEED_RNG": {
    "old_verdict_REJECTED": "CONFIRMED (position-keyed determinism, decompiled + cross-checked 76/76) — the iter033/034 state; the cross-check was CIRCULAR (the reference shared the JS assumptions: 32768.0, 2.0, 2.0f, no f32 rounding); the divisor/normalization knowledge was WRONG in the implementation",
    "corrected_verdict": "CONFIRMED — but ONLY ON THIS ITERATION'S NEW BASIS: the integer LCG identity (FUN_0098cdf0 seed + the MSVC rand() step) stands from iter032; the FLOAT64 operands are NOW BYTE-LOCKED (rand01 = f32(r/32767.0) with _DAT_00a7d7a8 = 32767.0 f64 @0x00A7D7A8, file 0x67D7A8, bytes 00 00 00 00 C0 FF DF 40); the lerp is f32 (FUN_0095ac30 FSTP DWORD @0x0095ACF0; the min/max are f32 fields); the sampler chain revalidated 76/76 BIT-EXACT by the ANTI-CIRCULAR reference (constants from the binary bytes, records from the ORIGINAL BNT, its own implementation)",
    "corrected_knowledge": "seed = ((p4*16+p5)*16+p1+p2+p3)*0x5CC7+0x6D7 (uint32), state = x*8^x; then MSVC rand() LCG state*0x343FD+0x269EC3 >>16 &0x7FFF (r in [0, 32767], always >= 0 — the 2^32f FADD idiom in FUN_0098ce30 is DEAD CODE); rand01 = f32(r / 32767.0 f64) -> [0, 1] INCLUSIVE (the FSTP DWORD @0x0098CE60 = the f32 rounding BEFORE return); the sampler value = f32(rand01*(max-min)+min); seed inputs: p1 = packed 32-bit query position, p2 = VIEW BAND (10/20/30) STRONGLY_SUPPORTED, p4/p5 = the record u16 pair; NO global seed, NO server RNG in the LOCAL spawn loop; the CELL-CONTENT ORIGIN remains open (the 'fully procedural, zero server RNG' claim stays BOUNDED to the local spawn-loop chain)",
    "demotions_reaffirmed": {
      "FOLIAGE_FULLY_PROCEDURAL_ZERO_SERVER_RNG": "STAYS DEMOTED (ledger #10): the accurate statement is 'the local spawn-loop chain is decoded; the cell-content origin remains open' — no overclaim",
      "seed_20030130": "VISUAL_RECONSTRUCTION_LEGACY (unchanged; the clean chain is position-keyed, different BY CONSTRUCTION)"
    },
    "historical_fidelity": "engine-byte-confirmed for 9.3.5 (the three QWORD operands + the six FSTP f32 points byte-locked this iteration; the bit-exactness PROVEN exhaustively over the complete input domains)",
    "evidence_status": "CONFIRMED (identity + arithmetic + rounding, iter035) / [P-RNG-P3] bounded (p3 = *(impl+0x24) UNVERIFIED -> 0); p2 field-level STRONGLY_SUPPORTED",
    "vector_note": "the human's byte-proven vector (RNG 9719: /32767 = 0.2966093935972167... vs the rejected /32768 = 0.296600341796875) is a BUILT-IN test of the revalidation and is LITERALLY instance 0's RNG draw on the deployed page (record col2/col3 = 0.5/1.5 -> value = 0.5 + rand01) — verified BIT-EXACT (iter035_rng_crosscheck.json)"
  },
  "scope_discipline": "per the NEXT_PROMPT: this iteration fixed ONLY the foliage chain; the OTHER human findings (the model witness, the water level datum, the regression-vs-original-client scope) are recorded as FOLLOW-UPS — not fixed here; the other flagged constants from the pattern re-check (iter035_pattern_findings.json) are follow-ups for their owning chains",
  "follow_ups_recorded": [
    "the original-direct model witness (decision #3's chain: original model -> geometry -> material -> texture/UV -> render — the foliage assets remain the GENERATED_CACHE GLBs)",
    "the water page level 0.0 vs the engine 10.0f datum tie",
    "the regression sweep being vs OUR recorded runtime, NOT vs the original client",
    "the 7 f32-widened-to-f64 constants with misleading low dwords (0.01, 0.3, 0.005, 0.2, 0.4, 0.7, 0.8 at 0x00A7B360/0x00A7B3E8/0x00A81D18/0x00A7B2D0/0x00A7B308/0x00A7B268/0x00A7AF78) — per-chain value verification for the material/water chains",
    "the iter027 'thresholds_doubles' width-label error (0x00A97C90 = 73.0 is f32, not f64; value-correct, label wrong)",
    "the visualizer path (FUN_0095ae20/FUN_0095b4f0 — the [0,1] node position/scale -> world transform + the rotation/variant candidates)"
  ]
}
