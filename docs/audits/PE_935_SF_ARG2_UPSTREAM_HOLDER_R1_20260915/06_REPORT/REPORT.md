# REPORT.md — PE_935_SF_ARG2_UPSTREAM_HOLDER_R1_20260915

RUN_CLASS: MATERIAL | RUN_TYPE: STATIC_SEAM_PROBE | MILESTONE: EU935-M1 | TARGET: PCG_9_3_5
Executor: pe-reconstruction (PE-MASTER direct bounded dispatch; NO_NESTED_TASKS; zero git mutations)
Executable: D:/Eudoria_Reconstruction/pcg_install/Entropia.exe — SIZE=8015872,
SHA256=E7785430E81DFFE648CE8F5312414B17BC9FCE61389689A22F753765D5280F31, PE32 i386,
ImageBase 0x00400000 (re-measured fail-closed by every generator; 01_RAW/IDENTITY_VERIFICATION.txt).
Binary NEVER executed. STATIC-ONLY.
Repo BASE_SHA: 895bbc8baa2d002c562b7e5b43212e38c2abb16f (start HEAD == origin == ls-remote; verified).

**R2 CORRECTION PROVENANCE (AMEND_LOG_R2.md):** the R1 package received
QC_VERDICT = QC_FAIL (06_REPORT/QC_AUDIT.md: one P0 — the R1 headline
"[ArkAnimation-family+0x14] is NULL at every statically-bounded construction;
no writer binds ANY object" was falsified by the QC's own bytes: at the
dispatcher factory blocks the `xor esi,esi` sits on the CONDITIONAL
allocation-failure path and the normal path bypasses it via
`jmp block_start+2` after constructing a child object). PE-MASTER assigned
this bounded correction batch (slice-engine fix + regeneration + re-derivation
+ P2/P3 fixes). All corrections are recorded in AMEND_LOG_R2.md; every
regenerated artifact was produced deterministically by the final script
versions; the census CSV and B.5 map regenerated byte-identically
(hash-for-hash) to the QC-verified R1 versions. Everything the QC confirmed
correct (06_REPORT/QC_AUDIT.md §6) is preserved.

## 0. PRE-REGISTERED HYPOTHESES (declared before results; criteria unchanged)

| H | statement (contract §9) | outcome | evidence |
|---|--------------------------|--------|----------|
| H1 | FUN_006FAB80 forwards two args to slot3 of [this+0x14]; falsifier = arg2 mutation or different slot/receiver path | **CONFIRMED** (arg2 verbatim via edx; arg1 bit-preserving x87 roundtrip; slot-3 byte evidence 8B 40 0C; ret 8; single basic block) | 01_RAW/FUN_006FAB80_DISASM.txt [A5][A7][A9] |
| H2 | [this+0x14] may hold a SceneFeederObject; falsifier = a writer proves the receiver is another unrelated RTTI class | **REJECTED — VIA ITS OWN PRE-REGISTERED FALSIFIER (R2 re-based)**: the writers DO bind objects — but every bounded receiver is an .?AVArkAnimationFloatValue@@-hierarchy channel class (Scale/Rotation/Translation/Intensity/Color/Alpha), none SceneFeederObject, and every child vtable slot-3 (0x006FFF00 / 0x009154A0) != 0x0050A050. The R1 basis ("NO writer binds ANY object") is retracted (QC P0-1). | 01_RAW/HOLDER14_WRITER_RAW.txt [IV-8][IV-9]; 02_ANALYSIS/RECEIVER_IDENTITY.md |
| H3 | at least one writer/source can prove exact receiver identity; falsifier = all writers opaque/computed | **CONFIRMED (R2 flip; was REJECTED in R1)**: 13 child-construction paths dispositioned — receiver classes RTTI-named with vtable VA + slot-3 dword measured for all 6 child classes over chunk closures; the child-construction + bypass edges byte-proven on all 13 paths; ret-this shape byte-proven on 11/13 (the 2 Alpha paths carry the ctor return-value caveat: construction proven, return-value shape not derived in-package). The R1 "all values constant NULL" was an engine artifact. | 01_RAW/HOLDER14_WRITER_RAW.txt [IV-8][IV-9] |
| H4 | forwarded arg2 has a traceable upstream source in a bounded caller window; falsifier = caller set unreachable/bound-exhausted | **REJECTED** — the caller set is EMPTY (0 direct E8/E9/EB, 0 proven dispatch candidates in 4 channels; no statically-identifiable caller) | 01_RAW/THUNK_CALLER_CENSUS.txt [D1..D4] |
| H5 | arg2 semantic role potentially narrower than generic string/name | **UNVERIFIED** — no arg2 value exists to classify; no semantic promotion attempted | 01_RAW/ARG2_VALUE_FLOW_RAW.txt [E4][E5] |

## 1. PRIMARY QUESTION — ANSWER (R2 CORRECTED)

> WHAT EXACTLY IS THE OBJECT HELD AT [ArkAnimation-family instance + 0x14] WHEN
> FUN_006FAB80 DISPATCHES SLOT 3 — and can we PROVE it is a SceneFeederObject?

**Answer (static, within the declared bound; R2 edge-aware engine,
AMEND_LOG_R2 R2-1/R2-2): A PER-CLASS TABLE — NOT UNIVERSAL NULL, NOT
SceneFeederObject.**

- TimeController, NodeUpdate, AnimatedTexture, ParticleSystemPredefined,
  ParticleSystemPredefinedMod: held = **NULL always** (immediate `push 0`
  at the base-ctor call sites 0x006FAFC0/0x006FB089/0x006FB402/0x006FE9E1;
  PSPMod chains via the PSP ctor's push 0 — byte-verified, R1 result stands).
- ArkAnimationDerivatives, ArkAnimationCyclicLinear, ArkAnimationCyclicSin
  (and ArkAnimationCyclic, which inherits the forwarded child): held =
  **NULL on the allocation-failure paths** (`xor esi,esi` behind the
  child-allocation NULL-test je) and = **a LIVE CHILD OBJECT on the normal
  paths**: the factory blocks construct a Scale / Rotation / Translation /
  Intensity / Color / Alpha child (`??2` -> child ctor -> `mov esi,eax` ->
  `jmp block_start+2` bypassing the xor) and pass the ctor's return as the
  derived ctor's arg2, which flows to the base ctor's arg2
  (`mov ecx,[esp+0xC]` @0x006FABA2) and into `mov [eax+0x14],ecx`
  @0x006FABB5. 13 xor-paths + 13 child-construction paths enumerated over
  the 8 factory call sites (01_RAW/HOLDER14_WRITER_RAW.txt [IV-8]).
  Ret-this shape is byte-proven on 11 of the 13 child paths (the five
  Scale/Rotation/Translation/Intensity/Color child ctors return their
  thiscall this — measured ret shapes in [IV-8]); on the 2 Alpha chains
  (child ctor 0x006FCD00 at call sites 0x006D213D/0x006D17BF) the
  construction is byte-proven (ctor call + vtable store 0x00A8663C +
  bypass edge; fresh ??2 allocation byte-proven at the 0x006D213D chain
  — push 0x1C -> ??2 @0x006D211E — while the 0x006D17BF chain's block
  je is the allocation-NULL test with the push pair outside the declared
  window, recorded honestly in [IV-8]) but the ctor's return-value shape
  is NOT derived in this package (its ret site is measured: 0x006FD268,
  ret 4): the held value on those 2 paths is the Alpha ctor's return —
  not proven NULL, and not shape-proven to be the Alpha object.
- All six child classes are RTTI-identified (.?AVArkAnimationScale@@ /
  Rotation / Translation / Intensity / Color / Alpha) and ALL root at
  .?AVArkAnimationFloatValue@@ — a SEPARATE hierarchy root from
  .?AVArkAnimationPredefined@@: the 10-class family closure STANDS (the
  correction concerns the FIELD's value, not the family's definition).

The SceneFeederObject linkage is therefore **REJECTED AT THE
RECEIVER-IDENTITY LINK**: the binding edge is REAL and LIVE (a composite/
channel-animation binding mechanism), but the receivers are the
FloatValue-family channel classes, whose vtable slot-3 dwords
(0x006FFF00 for Scale/Rotation/Translation; 0x009154A0 for
Intensity/Color/Alpha — each measured from the pinned bytes) are NOT
0x0050A050. A slot-2 dispatch on a live composite would forward arg1/arg2 to
the CHILD's slot-3 — never to FUN_0050A050 (the R1 crash-consistency claim
"a dispatch would fault at [NULL]" is RETRACTED — a dispatch on a live
composite would NOT fault). The SceneFeederObject infrastructure itself
remains fully byte-validated (vtable 0x00A7D458 = .?AVSceneFeederObject@@,
slot-3 FUN_0050A050, slot-17 chain) — but nothing binds it into the family
field. The slot-2 forwarder itself still has NO STATICALLY-IDENTIFIABLE
CALLER (Phase D: 0 direct calls, 0 imm32 references beyond its own five
vtable slot dwords, 0 proven dispatch sites among 1,438 slot-2 candidates;
the construction layer IS statically reachable — the dispatcher construct
entry 0x006D0ED0 has 3 direct E8 callers 0x0058EFD6/0x0058F19F/0x006D2B1C,
re-derived; the R1 "unexercised stub layer" reading is RETRACTED).

## 2. HEADLINE OUTCOMES (contract §16 fields)

- **FORWARDER_OPERATION_STATUS: CONFIRMED** (10/10 checks; unchanged; 01_RAW/FUN_006FAB80_DISASM.txt [A9]).
- **Five-class membership result**: exactly the five pinned RTTI classes,
  **slot ordinal 2 in ALL five**; no corrected count, no extras; 5 IMM32 hits
  total (01_RAW/RTTI_COL_RAW.txt [B1][B2][B4]). Shared base =
  .?AVArkAnimationPredefined@@; family closure = 10 classes.
- **Writer census denominators (UNCHANGED, QC-re-verified)**: 10,711 rows
  total; 1,898 REG_INDIRECT; 8,811 STACK_FRAME (excluded, counted); 2 SIB;
  0 ABSOLUTE; 0 UNKNOWN. **FAMILY_CONTEXT_PROVEN = 3** (criterion a=3,
  criterion b=0). (01_RAW/HOLDER14_CENSUS_SUMMARY.txt [C5][C6].)
  R2: the census CSV and B.5 map regenerated BYTE-IDENTICAL to the R1
  QC-verified versions (SHA256 DBD069EC… / CD436256… — determinism evidence).
- **HELD_OBJECT_IDENTITY (R2 CORRECTED): the per-class table** — NULL always
  for 5 classes; for Derivatives/CyclicLinear/CyclicSin(/Cyclic): NULL on
  allocation-failure paths, LIVE CHILD OBJECT (FloatValue-family channel
  class) on normal paths. Ladder fully traversed: L0 (new-allocation
  source) -> L1 (child vtable stores) -> L2 (RTTI names) -> L3 (slot-3
  targets measured) -> **L4 NEGATIVE** (slot-3 != 0x0050A050 for every child
  class). Ret-this shape byte-proven on 11/13 child paths; the 2 Alpha
  paths construction-proven with the ctor return-value shape not derived
  in-package. (01_RAW/HOLDER14_WRITER_RAW.txt [IV-8][IV-9].)
- **SCENEFEEDER_LINK: REJECTED — RE-BASED AT THE RECEIVER-IDENTITY LINK**
  (the binding edge exists and is live; the receivers are the FloatValue-
  hierarchy channel classes; strong-positive test A/B/C all NOT MET — B
  executes and rejects). (02_ANALYSIS/RECEIVER_IDENTITY.md.)
- **THUNK_CALLERS census outcome (UNCHANGED)**: E8/E9/EB byte denominators
  156,829 / 40,668 / 17,961 -> 0 hits; imm32 = 5 (own slots); vtable
  memberships = 5; dispatch-pattern candidates = 1,438 -> PROVEN 0 /
  REJECTED 5 / INSUFFICIENT 1,433. (01_RAW/THUNK_CALLER_CENSUS.txt [D1..D4].)
- **ARG2_PROVENANCE: NOT_DEMONSTRATED** (empty caller set; the R1
  "no arg2 can ever reach a live receiver" justification is retracted —
  the NOT_DEMONSTRATED stands on the empty caller set alone).
- **ARG2_VALUE_CLASS: NOT_DEMONSTRATED** (no value; no STRING claim anywhere).
- **ARG2_SEMANTIC_ROLE: UNVERIFIED** (valid G11 outcome).
- **DOWNSTREAM_CONTINUITY (G12): NOT_APPLICABLE** (receiver != SceneFeeder;
  the NiNode slot-17 pin value was recorded in 01_RAW/IDENTITY_VERIFICATION.txt
  [5] as a pins-table revalidation only — R2: the printed pin address
  corrected to 0x00A8CD38 per QC P3-4, value 0x007B5390 stands).
- **TOP_NEW_DISCOVERY (R2 CORRECTED)**: the held field is a **LIVE
  COMPOSITE / CHANNEL-ANIMATION BINDING MECHANISM** — an ArkAnimation
  family object holds an .?AVArkAnimationFloatValue@@-hierarchy channel
  child (Scale / Rotation / Translation / Intensity / Color / Alpha) at
  +0x14, refcounted at child+4 and released via child->vt[0](1); the
  dispatcher constructs the child per animation-name case and passes the
  ctor's return through the derived ctor's arg2 chain (ret-this shape
  byte-proven on 11/13 child paths; the 2 Alpha chains: construction
  byte-proven, ctor return-value shape not derived in-package); the slot-2
  thunk would forward
  arg1/arg2 to the child's slot-3. The construction layer is STATICALLY
  REACHABLE (dispatcher entry 0x006D0ED0, 3 direct E8 callers). Only the
  slot-2 forwarder remains without a statically-identifiable caller
  (R2 qualification per QC P2-1).
- **TOP_FALSIFIED_HYPOTHESIS**: H2 (SceneFeederObject held at
  [ArkAnimation+0x14]) — falsified VIA ITS OWN PRE-REGISTERED FALSIFIER:
  writers bind OTHER RTTI classes (the FloatValue-family channel children),
  whose slot-3 != 0x0050A050.
- **TOP_OPEN_BLOCKER**: none for the primary question (it is answered);
  residual uncertainty for any future re-opening: (i) 1,433 slot-2 dispatch
  candidates with unproven receivers (none proven family; R2: the fault
  shortcut "all family objects carry held=NULL so a dispatch would fault" is
  DROPPED — a dispatch on a live composite would forward to the child's
  slot-3, it would NOT fault); (ii) 68 PARAM_CHAIN / 820 THIS_ENTRY
  non-family census rows whose bases are unproven in the declared bound
  (none family-proven by fingerprint/slice/class; R2 [C8] histogram sums to
  exactly 1,895); (iii) computed-alias / runtime-patched writes are outside
  static reach (declared failure classes); (iv) R2-declared bulk-copy
  channel bound: 618 rep movs/stos sites decoded in .text, 4 in the family
  code cluster — NONE touches a family object's +0x14; .text-wide
  destination-provenance tracing of the remaining 614 sites is DECLARED
  NOT_CHECKED (01_RAW/HOLDER14_WRITER_RAW.txt [VIII]).

## 3. FINDINGS (P0–P3; R2-corrected)

**P0 — headline (primary question answered; R2 CORRECTED):**
- P0-1 (R2): [ArkAnimation-family+0x14] holds NULL always for 5 classes and,
  for the 4 param-chained classes, NULL on allocation-failure paths and a
  LIVE RTTI-IDENTIFIED CHILD OBJECT (Scale/Rotation/Translation/Intensity/
  Color/Alpha; .?AVArkAnimationFloatValue@@ hierarchy) on normal paths;
  writer set exhaustively = {ctor := arg2 (per-class table), dtor := 0,
  deleting-dtor := 0}; HELD_OBJECT_IDENTITY = the per-class table; the
  receiver-identity ladder reaches L3 with L4 NEGATIVE for every child
  class. 13 child paths: ret-this shape byte-proven on 11/13 — the 2 Alpha
  paths are construction byte-proven with the ctor return-value shape not
  derived in-package (the held value there is the Alpha ctor's measured
  return: not proven NULL, not shape-proven to be the Alpha object).
  Evidence: [IV-8]/[IV-9] in HOLDER14_WRITER_RAW.txt. (The R1 P0-1
  "NULL at every statically-bounded construction" is superseded — see
  AMEND_LOG_R2 R2-3.)
- P0-2: FUN_006FAB80 has zero statically-identifiable callers; the slot-2
  forwarder is dead code in static reach (R2 qualification per QC P2-1: the
  census proves no STATICALLY-IDENTIFIABLE caller; 1,433 INSUFFICIENT_PROOF
  candidates remain unproven, not disproven). Evidence: THUNK_CALLER_CENSUS.txt.
- P0-3: SCENEFEEDER_LINK REJECTED at the receiver-identity link (re-based).
  SF infrastructure itself validated byte-for-byte (N3/N4/N5).

**P1 — pin corrections (all re-derived, never silently adopted):**
- P1-1 (PIN-CTOR ABI): shared ctor takes THREE stack args (ret 0xC):
  arg0=byte flag->+0x28, arg1=float->+0x2C, arg2=HELD->+0x14 (read at
  [esp+0xC]). The prior attribution of a single param was wrong; held = arg2.
  The pin's byte sequences (89 48 14 / BA 01 00 00 00 / 01 51 04) all
  validated, with the addition of a NULL-guard (test ecx,ecx; je) around the
  refcount increment that the pin did not note.
- P1-2 (PIN-FIVE double membership; R2 wording per QC P3-8): the prior run's
  second `.?AVArkAnimationCyclic@@` vtable membership at 0x00AA7958 is NOT
  reproduced — the exhaustive IMM32 census contains no thunk dword there
  (5 hits total). Own re-measurement: the CYCLIC CHD sits at 0x00AA7948
  (sig=0, numBases=2); its pBCD-array pointer field @0x00AA7954 points to
  the array at 0x00AA7958, whose first entry (0x00AA7964) is CYCLIC's own
  base-class descriptor; CyclicLinear's COL is 0x00AA7980 and its hierarchy's
  base array begins at 0x00AA79A4 — 0x00AA7958 is RTTI data, not a vtable;
  no 0x006FAB80 dword in 0x00AA7930..0x00AA79A4.
- P1-3 (PIN-SF2 extent): FUN_0050A050 spans TWO B.5 chunks
  ([0x0050A050..0x50A087) + [0x50A087..0x50A0AA)) — pin extent
  0x0050A050..0x0050A0AA CONFIRMED including both ret-8 sites (0x50A084;
  0x50A0A7 in the second chunk, je-fallback path dispatching SF slot-1 and
  copying a float3 into arg1's out-buffer).
- P1-4 (PIN-SFCHAIN container): the "+0x10 store receiving FUN_005247C0
  result" at 0x0044D680 is inside function [0x0044D5D8..0x0044D787), not
  FUN_0044D590 (B.5 extent [0x0044D590..0x0044D5D8) is a guard fragment).
  The real container stores three factory results at +0x0C/+0x10/+0x14
  (factory calls with args 0xF0000000/1/2) — three SceneFeederObject
  instances per container.
- P1-5 (family scope; R2 CORRECTED value part): the family is 10 classes,
  not 5: Predefined + 6 direct-derived (TimeController, NodeUpdate,
  AnimatedTexture, Derivatives, ParticleSystemPredefined, Cyclic) + 3
  (Linear, Sin via Cyclic; ParticleSystemPredefinedMod). All construct with
  held=NULL — RETRACTED; corrected: 5 classes bind NULL always; the 4
  param-chained classes bind NULL on the failure paths and a live child
  object on the normal paths ([IV-9]).

**P2 — methodology/capability findings (for future runs):**
- P2-1: capstone 5.0.7 (python, x86) misreports x87 STORE access
  (`fstp [mem]` -> access=READ). Architectural override required for
  write-censuses. Caught by capability probe (IDENTITY_VERIFICATION.txt [3]).
- P2-2 (magnitude retracted in the R2 packaging batch; see AMEND_LOG_R2
  R2-6): capstone 5.0.7 `Cs.disasm()` per-call setup makes per-function
  decode loops pathological; prefer streaming/batched decoding (the map
  pass = a single stream). The R1-era "~25–35 ms fixed per-call setup
  cost" magnitude is RETRACTED as unreproducible (external audit +
  PE-MASTER benchmark on python 3.12.7 / capstone 5.0.7: ~0.03 ms warm
  per-call on small buffers, ~0.1 ms with fresh Cs objects, ~6 ms to
  first insn of a 1 MB stream). Exact magnitudes are environment-dependent
  and NOT load-bearing; the operational lesson (single-stream decode
  passes are the workable design) stands on observed run behavior only.
- P2-3: B.5 tight boundaries chunk multi-exit functions (e.g. SF ctor
  0x509330|0x5093C1; FUN_0050A050 two chunks). Attribution and ABI analysis
  must join chunks; documented in AMEND_LOG_R1.md and, for the R2
  measurements, in the chunk-closure rule (AMEND_LOG_R2 R2-2).
- P2-4: 5 esp/ebp-based SIB rows are counted in STACK_FRAME (category
  precedence) and 2 non-frame SIB rows enumerated separately — both
  documented in NEGCTL_RECEIVER_RAW.txt [N6].
- P2-5 (R2, from the QC): the R1 slice engine's function-entry assumption
  (registers fresh at B.5 pseudo-function starts; callers == E8 callers) is
  INVALID inside branch-target constructs — the R2 edge-aware engine
  (EdgeContext; MULTI_PATH/MULTI_ENTRY at block boundaries; declared
  construct [0x006D0ED0..0x006D265F); test/cmp read-model; this-cross)
  fixes it (AMEND_LOG_R2 R2-1). The R1 4-level bound was widened to a
  declared 6 for the anchor slices (R2-2).

**P3 — minor/contextual (R2 additions marked):**
- P3-1: sibling delegation stubs measured: slot 6 (0x006FAB40) -> self-vt
  slot 4; slot 7 (0x006FAB60) -> self-vt slot 5 (its `[eax+0x14]` read is a
  VTABLE-slot-5 read, not an object-field read).
- P3-2: SF slot 0 (0x0050A460) is a scalar deleting dtor — ABI-compatible
  with the family dtor's held release `held->vt[0](1)`; compatibility noted,
  no identity claim (R2: the actual children carry their own slot-0 methods).
- P3-3: SceneFeederObject derives from ArkAudioObjectInterface (COL
  hierarchy) — context for future SF work.
- P3-4: family object sizes measured: NodeUpdate 0x40, TimeController 0x44,
  AnimatedTexture 0x54, Derivatives 0x6C, CyclicSin 0x88, CyclicLinear 0xC4;
  SceneFeederObject 0x98 (SF factory new) with an additional 0x118 object
  allocated inside the SF ctor. R2: PSP and PSPMod measured at 0x70
  (cascade-size provenance, [IV-0d]); child sizes: Scale 0xC, Rotation 0x7C,
  Color 0x30, Translation 0x24, Alpha 0x1C, Intensity 0x48 ([IV-8]).
- P3-5 (R2, QC P3-x fixes recorded in AMEND_LOG_R2 R2-4): [N7] regenerated
  over the DECLARED 57-function family set with chunk closures (22 refs);
  [C4] completed to the full family-ctor caller census; ArkVegetationObservable
  slot-2 print corrected to the real dword (0x566B7241 = ASCII "ArkV" — the
  vtable has 1 slot); PIN-NN printed address corrected to 0x00A8CD38;
  the AT_RUN_END local-timestamp corrected (07:06:59Z = 2026-09-15 00:06:59
  PDT); the AT_RUN_START dangling pointer re-pointed to AMEND_LOG_R1 A8;
  [IV-0d] completed with the PSP + PSPMod new-sites.

## 4. GATES SELF-ASSESSMENT (full table in 06_REPORT/STAGE_ACCEPTANCE_GATES.csv)

G0 PASS (identity re-measured == pins, fail-closed) |
G1 PASS (BASE_SHA verified at start; end observation recorded; zero
mutations; untracked set = 2 pre-existing dirs + this run dir; R2: re-verified
read-only after the correction batch — HEAD still 895bbc8, zero mutations) |
G2 PASS (Phase A complete; CONFIRMED) |
G3 PASS (five names + vtables + extents + ordinals + ctors/dtors + hierarchy;
corrections P1-1/P1-2 documented) |
G4 PASS (denominator 10,711; every row classified; full writer fields;
no drops; R2: census CSV + B.5 map regenerated byte-identical to the
QC-verified versions) |
G5 PASS (all writer value sources dispositioned on the ladder — R2: the ctor
writer's sources climb L0->L1->L2->L3->L4-NEGATIVE per child chain; the
5-class NULL sources terminate at L0; explicit level assignment per writer) |
G6 PASS (strong-positive test A/B/C executed under the corrected values —
A not met (child vtables stored, not 0x00A7D458); B executes and REJECTS
(RTTI-identified sources whose slot-3 != 0x0050A050); C not met (no SF
chain); CONFIRMED not reached -> SCENEFEEDER_LINK REJECTED at the
receiver-identity link) |
G7 PASS (1/803 selectivity; PIN-NC anchors re-derived;
FAILURE_CASE_DETECTED=YES) |
G8 PASS (4 channels with denominators + per-candidate classification) |
G9 PASS via NOT_APPLICABLE (no proven caller; strongest-bounded candidates
dispositioned) |
G10 PASS with honest NOT_DEMONSTRATED |
G11 PASS (UNVERIFIED — valid outcome) |
G12 NOT_APPLICABLE (receiver != SceneFeeder — receivers are the FloatValue-
hierarchy children) |
G13/G14/G15 PENDING (owned by QC session / PE-MASTER / pe-master-auditor).

## 5. STOP CONDITION

**SUCCESS B PROPER (R2-corrected reading)**: the held receiver class is
proven NOT SceneFeeder -> the Rosetta seam is falsified AT THE
RECEIVER-IDENTITY LINK -> the ACTUAL receiver classes ARE identified
(.?AVArkAnimationScale@@ / Rotation / Translation / Intensity / Color /
Alpha — the .?AVArkAnimationFloatValue@@ channel hierarchy; slot-3 targets
0x006FFF00 / 0x009154A0, none FUN_0050A050). The binding edge EXISTS and is
exercised at every bounded normal construction (a live composite-animation
mechanism); the R1 formulation "the receiver is NULL / the binding edge does
not exist" is superseded. **SUCCESS C is also satisfied** (all statically
reachable +0x14 writer classes exhaustively bounded; the exact remaining
uncertainty localized — §2 TOP_OPEN_BLOCKER). Science stopped here per
contract §8 (no endless expansion). Arg2 upstream is dispositioned
NOT_DEMONSTRATED (empty caller set — SUCCESS D not applicable).

## 6. SELF-CHECK (executor's own, labelled SELF_CHECK; NOT an independent audit)

- Every generator re-measured identity first (S0 PASS lines in all raws; the
  R2-regenerated raws carry the CORRECTION header line).
- Denominators: census 10,711 (the R2 regeneration reproduced the R1 CSV and
  B.5 map hash-for-hash: DBD069EC… / CD436256… — dual-run determinism);
  E-lattice cross-checked by raw-byte scan (6 shared-ctor sites, 0 hidden).
- The P0 counterexample was re-derived from own bytes BEFORE any edit
  (scratch verification rounds; the QC's anchor values — factory-block
  shape, child ctor vtables/RTTI, slot-3 dwords 0x006FFF00/0x009154A0,
  dispatcher E8 callers — all reproduced; the R2 bound additionally
  discovered 2 further alias chains the QC table did not list: Rotation and
  Color into the Sin-4 shared tail, and Rotation/Translation/Alpha into the
  Deriv-C shared tail, enumerated in [IV-8]).
- Negative controls: G7 classifier selectivity 1/803 + 2 PIN-NC anchors +
  5 REJECTED dispatch candidates re-verified != thunk (R2: the
  ArkVegetationObservable line now prints the real dword 0x566B7241 "ArkV").
- No default-success fallbacks: H2 honestly re-based on its own falsifier;
  H3 flipped to CONFIRMED with full byte evidence; H4/H5 honestly
  REJECTED/UNVERIFIED; G10 NOT_DEMONSTRATED; 1,433 INSUFFICIENT_PROOF
  recorded with the fault-shortcut DROPPED; the bulk-copy channel's
  .text-wide tracing is declared NOT_CHECKED, not assumed away.
- Honest boundary: all conclusions are STATIC, within the declared bounds;
  runtime/computed-alias effects are declared out of reach, not assumed away.

RUN_STATUS: **PROPOSAL_READY** (no hard stop; science complete and bounded;
R2 correction batch applied per the PE-MASTER-ordered bounded correction
contract; see AMEND_LOG_R2.md for the full defect -> fix -> regeneration ->
re-derivation chain).
