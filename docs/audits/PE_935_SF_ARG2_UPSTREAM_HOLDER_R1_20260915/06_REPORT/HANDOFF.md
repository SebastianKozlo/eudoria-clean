# HANDOFF.md — PE_935_SF_ARG2_UPSTREAM_HOLDER_R1_20260915

RUN_STATUS: PROPOSAL_READY | HARD_STOP_REASON: (none)
Compact notice to PE-MASTER (full detail in REPORT.md; corrections ledger:
00_CONTROL/AMEND_LOG_R2.md):

## What was executed

Static-only (binary never executed) seam probe of FUN_006FAB80 and the
ArkAnimation family's [this+0x14] holder, all from the pinned Entropia.exe
bytes (G0 re-measured by every generator): Phase A thunk decode; Phase B
RTTI/class map; Phase C full-.text +0x14 writer census + edge-aware
anchor-chain slices (declared 6 levels x 384B, branch-edge aware inside the
dispatcher construct [0x006D0ED0..0x006D265F)) + upstream continuation;
Phase D four-channel thunk-caller census; Phase E disposition; G7 negative
control; SF-chain revalidation. Zero git mutations. History: R1 run ->
fresh QC QC_FAIL (one P0; 06_REPORT/QC_AUDIT.md) -> CORRECTION_BATCH_R2
(science; AMEND_LOG_R2 R2-1..R2-3) -> PACKAGING_AND_PROVENANCE_CORRECTION
(this batch; AMEND_LOG_R2 R2-6): correction ledger created (51 dangling
references now resolve), stale R1 texts regenerated, script/manifest
registries re-measured, __pycache__ removed, Alpha ret-this prose narrowed
(11/13 shape-proven; 2/13 construction-proven with untraced return value),
capstone timing magnitude retracted as unreproducible.

## Headline outcomes (R2-corrected)

- FORWARDER_OPERATION_STATUS: **CONFIRMED** (10/10 checks;
  01_RAW/FUN_006FAB80_DISASM.txt [A9]).
- Five-class membership: **exactly the five pinned RTTI classes, slot
  ordinal 2 in ALL five**; shared base .?AVArkAnimationPredefined@@;
  10-class family closure; 5 IMM32 hits total
  (01_RAW/RTTI_COL_RAW.txt [B1][B2][B4]).
- Writer census: **denominator 10,711 rows** (1,898 REG_INDIRECT / 8,811
  STACK / 2 SIB / 0 ABS / 0 UNKNOWN); **FAMILY_CONTEXT_PROVEN = 3**
  (a=3, b=0); values: ctor := arg2 (per-class table), dtor := 0,
  deleting-dtor := 0; census CSV + B.5 map regenerated byte-identically to
  the QC-verified versions (SHA256 DBD069EC… / CD436256…).
- **HELD_OBJECT_IDENTITY: the PER-CLASS TABLE** — NULL always for
  TimeController, NodeUpdate, AnimatedTexture, ParticleSystemPredefined,
  ParticleSystemPredefinedMod; for Derivatives / CyclicLinear / CyclicSin
  (Cyclic inherits via forwarding): NULL on the allocation-failure paths,
  LIVE RTTI-IDENTIFIED CHILD OBJECT (Scale / Rotation / Translation /
  Intensity / Color / Alpha — the .?AVArkAnimationFloatValue@@ channel
  hierarchy) on the normal paths; 13 xor-paths + 13 child-paths over 8
  factory call sites; ret-this shape byte-proven on 11/13 child paths
  (the 2 Alpha paths: construction byte-proven — ctor call + vtable store
  0x00A8663C + bypass edge; the fresh ??2 allocation is byte-proven on
  the Deriv-C chain and recorded honestly as outside the declared window
  on the Sin-2 chain; ctor ret site measured 0x006FD268 — but its
  return-value shape is NOT derived in this package; the held value there
  is the Alpha ctor's return: not proven NULL, not shape-proven to be the
  Alpha object)
  (01_RAW/HOLDER14_WRITER_RAW.txt [IV-8][IV-9]).
- **HOLDER_BINDING_MECHANISM: REAL** — a live composite/channel-animation
  binding mechanism (refcounted at child+4, released via child->vt[0](1));
  construction layer STATICALLY REACHABLE (dispatcher entry 0x006D0ED0, 3
  direct E8 callers 0x0058EFD6/0x0058F19F/0x006D2B1C).
- **SCENEFEEDER_LINK: REJECTED AT THE RECEIVER-IDENTITY LINK** — the
  binding edge is real and live, but the receivers are the
  FloatValue-hierarchy channel classes whose vtable slot-3 dwords
  (0x006FFF00 / 0x009154A0, each measured from the pinned bytes) are NOT
  0x0050A050; strong-positive test A/B/C all NOT MET (B executes and
  REJECTS); a slot-2 dispatch on a live composite would forward arg1/arg2
  to the CHILD's slot-3, never to FUN_0050A050
  (02_ANALYSIS/RECEIVER_IDENTITY.md).
- THUNK_CALLERS: **zero PROVEN / 5 REJECTED / 1,433 INSUFFICIENT_PROOF**
  within the declared census (E8/E9/EB byte denominators
  156,829/40,668/17,961 -> 0; imm32 = 5 own slots; memberships = 5;
  1,438 dispatch-pattern candidates) — the slot-2 forwarder has no
  statically-identifiable caller (qualified per QC P2-1).
- ARG2_PROVENANCE / ARG2_VALUE_CLASS: **NOT_DEMONSTRATED** (stands on the
  empty caller set alone); ARG2_SEMANTIC_ROLE: **UNVERIFIED**.
- DOWNSTREAM_CONTINUITY (G12): **NOT_APPLICABLE** (receiver != SceneFeeder
  — receivers are the FloatValue-hierarchy children; the NiNode slot-17
  pin value recorded in 01_RAW/IDENTITY_VERIFICATION.txt [5] as a
  pins-table revalidation only, printed address 0x00A8CD38).
- TOP_NEW_DISCOVERY: the live composite/channel-animation binding
  mechanism (family objects hold FloatValue-hierarchy channel children at
  +0x14; per the R2-corrected REPORT §2 wording).
- TOP_FALSIFIED_HYPOTHESIS: H2 — falsified VIA ITS OWN PRE-REGISTERED
  FALSIFIER (writers bind other RTTI classes; slot-3 identity mismatch),
  NOT via "no writer binds any object".
- TOP_OPEN_BLOCKER: none for the primary question; residual: (i) 1,433
  unproven-receiver dispatch candidates (no fault-shortcut); (ii) 68
  PARAM_CHAIN / 820 THIS_ENTRY non-family rows unproven in the declared
  bound ([C8] histogram sums to exactly 1,895); (iii) computed-alias /
  runtime-patched writes outside static reach; (iv) bulk-copy channel
  bound: 618 rep movs/stos sites in .text, 4 in the family cluster (none
  touches +0x14), 614 out-of-cluster destination-provenance DECLARED
  NOT_CHECKED (01_RAW/HOLDER14_WRITER_RAW.txt [VIII]).

## Gates (self-assessment; corrected 06_REPORT/STAGE_ACCEPTANCE_GATES.csv)

G0 PASS, G1 PASS, G2 PASS, G3 PASS (corrections P1-1/P1-2), G4 PASS, G5
PASS (R2-corrected ladder: per-child-chain L0->L1->L2->L3->L4-NEGATIVE;
5-class NULL at L0; dtor imm 0), G6 PASS (A/B/C executed under the
corrected values; CONFIRMED not reached -> REJECTED at the
receiver-identity link), G7 PASS (FAILURE_CASE_DETECTED=YES; 1/803
selectivity), G8 PASS, G9 PASS (NOT_APPLICABLE), G10 PASS
(NOT_DEMONSTRATED), G11 PASS (UNVERIFIED), G12 NOT_APPLICABLE. G13
chronology: R1 QC_FAIL (06_REPORT/QC_AUDIT.md) -> CORRECTION_BATCH_R2
(AMEND_LOG_R2 R2-1..R2-3) -> PACKAGING_AND_PROVENANCE_CORRECTION (this
batch; AMEND_LOG_R2 R2-6) -> RE-QC PENDING (owned by the fresh
pe-master-auditor re-QC session). G14/G15 PENDING.

## Key pin corrections P1-1..P1-5 (current R2 content)

- P1-1: shared ctor ABI = thiscall, ret 0xC, held = **arg2** ([esp+0xC]),
  arg0=byte flag, arg1=float; refcount inc has NULL-guard.
- P1-2: prior ".?AVArkAnimationCyclic@@ second vtable 0x00AA7958" NOT
  reproduced (RTTI data region — the Cyclic CHD's pBCD-array field; no
  thunk dword; count remains 5; R2 wording per QC P3-8).
- P1-3: FUN_0050A050 = two B.5 chunks; pin extent confirmed incl. second
  ret-8 site 0x50A0A7 (je-fallback dispatches SF slot-1 -> float3
  out-copy).
- P1-4: SF container +0x10 store @0x0044D680 lives in function
  0x0044D5D8..0x0044D787 (not FUN_0044D590); container binds THREE
  SceneFeederObjects (+0x0C/+0x10/+0x14).
- P1-5: family = 10 classes; value part R2-corrected (per-class table; the
  R1 "all construct with held=NULL" is retracted).
- P2-1 (kept): capstone 5.0.7 x86 misreports fstp [mem] access as READ
  (override required for write-censuses).
- P2-2 (DOWNGRADED in the R2 packaging batch; AMEND_LOG_R2 R2-6):
  capstone 5.0.7 `Cs.disasm()` per-call setup makes per-function decode
  loops pathological — prefer streaming/batched decoding (the map pass =
  a single stream); the R1-era "~25–35 ms fixed per-call cost" magnitude
  is RETRACTED as unreproducible (PE-MASTER benchmark on python 3.12.7 /
  capstone 5.0.7: ~0.03 ms warm per-call, ~0.1 ms fresh-Cs, ~6 ms to
  first insn of a 1 MB stream) — operational lesson only, no magnitude;
  exact numbers are environment-dependent and NOT load-bearing.

## Package

docs/audits/PE_935_SF_ARG2_UPSTREAM_HOLDER_R1_20260915/
- 00_CONTROL/ (RUN_CONTRACT.md verbatim, SOURCE_IDENTITIES.json,
  SCRIPT_SHA256.csv, AMEND_LOG_R1.md, **AMEND_LOG_R2.md**, scripts/ — 10
  scripts, no __pycache__)
- 01_RAW/ (16 raw artifacts incl. all contract-listed raws)
- 02_ANALYSIS/ (5 files incl. SCIENCE_STATUS_DELTA.csv)
- 03_EVIDENCE/ (README.md, EVIDENCE_INDEX.csv — claims C1..C23)
- 06_REPORT/ (REPORT.md, STAGE_ACCEPTANCE_GATES.csv, MANIFEST_SHA256.csv,
  QC_AUDIT.md — the R1 QC historical record, intentionally outside the
  manifest per the R1 convention, this HANDOFF.md)

Next steps for the parent (suggestions, not self-promotions): fresh re-QC
audit (G13), PE-MASTER adjudication (G14), persistence via
pe-master-auditor (G15). The PRIMARY QUESTION is answered within the
declared bound; H2-dependent Rosetta-chain work downstream of the family
seam (NiNode slot-17, world position consumers) remains untouched per
scope discipline.
