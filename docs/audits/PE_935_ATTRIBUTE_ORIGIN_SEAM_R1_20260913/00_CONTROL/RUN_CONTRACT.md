# RUN_CONTRACT — PE_935_ATTRIBUTE_ORIGIN_SEAM_R1_20260913 (werbatim prompt dispatchu)

RUN_ID: PE_935_ATTRIBUTE_ORIGIN_SEAM_R1_20260913
EXECUTOR: pe-reconstruction (bezpośredni dispatch PE-MASTER; NO_NESTED_TASKS — żadnych sub-agentów; wyniki cząstkowe zapisuj na dysk i kontynuuj).
RUN_CLASS: LOAD_BEARING. TRYB: STATIC-ONLY — zakaz uruchamiania klienta/Fridy/x32dbg/mock-serwera/połączeń sieciowych.

ERA: EU 9.3.5 (pcg_install). Binarium: D:\Eudoria_Reconstruction\pcg_install\Entropia.exe, SHA256 E7785430E81DFFE648CE8F5312414B17BC9FCE61389689A22F753765D5280F31 (8 015 872 B, base 0x00400000, ASLR OFF). templates.vfs: SHA256 BE57818C7516F8C6C8A68DF427591567DD0E5A421934AC24ADA57E8261F65B77. Asercja fail-closed S0 obu SHA (01_RAW\S0_ERA_ASSERTION.json; mismatch = HARD STOP).

REPO: D:\Eudoria_Reconstruction\12_WebGame\eudoria-clean. BASE_SHA = 5d0edde5863795d6eefd8bb11989e8edb7b28546 (HEAD==origin/master, zweryfikowane przez PE-MASTER; nie wykonujesz operacji git — commit/push zrobi pe-master-auditor po QC). Dirty: obce ?? experiments/ — POZA ZAKRESEM.

AUDIT_OUTPUT_ROOT (zweryfikowany przez PE-MASTER jako nieistniejący przy SETTLE; sprawdź ponownie, przy kolizji dodaj sufiks _R2):
D:\Eudoria_Reconstruction\99_Audits\PE_935_ATTRIBUTE_ORIGIN_SEAM_R1_20260913\
Struktura: 00_CONTROL\ (skrypty + GHIDRA_LOCAL kopia z pakietu A), 01_RAW\ (dumpy/censusy/wyniki), 02_ANALYSIS\ (mapa przepływu + macierz kluczy), 06_REPORT\ (RESEARCH_FINDINGS.md roboczy).

Ghidra: skopiuj projekt z D:\Eudoria_Reconstruction\99_Audits\PE_935_RECEIVER_PROVENANCE_CORRECTIONS_R1_20260913\00_CONTROL\GHIDRA_LOCAL do B\00_CONTROL\GHIDRA_LOCAL (kontrola braku locka; jeden writer; asercja SHA binarium; manifesty SHA zapisane — NIE powtarzaj błędu stale-manifest: każdy manifest opisuje dokładnie stan, który pokrywa).

GŁÓWNE PYTANIE (jedno): SKĄD trafia konkretna wartość pozycji/rotacji do właściwego kontenera i jak klucz/kontener wiąże się z TĄ samą instancją i modelem? Ścieżka = jeden rzeczywisty szew: MAPA PARAMETRÓW (singleton DAT_00BA12E8, getter FUN_004154F0, resolver FUN_008544D0: klucz→[hit+8], wartości layout {+0 vtable, +4 nullable lock, +8 small-int wariantu}) — KTO wstawia wartości, JAKA klasa jest wartością, JAKIE klucze, SKĄD dane.

ZADANIA (kolejność; każde z own-bytes + granicami instrukcji + pełnymi ciałami, nie samymi prologami):
1. KLASA WARTOŚCI MAPY (zamknięcie RECEIVER_UNRESOLVED #1 z Fazy A): (a) znajdź w .text rzeczywiste wywołania resolvera FUN_008544D0, które zwracają [hit+8] do kodu czytającego [value+0] — i zdumpuj [+0]-vtable wartości z live przykładu w dekompilacie (fun_00514EF0/FUN_008553D0 regiony już znane); (b) zidentyfikuj klasę wartości przez vtable→COL→RTTI (metodą TD z Fazy A); (c) zdekompiluj ctor tej klasy — jakie pola przyjmuje, kto go woła (census); (d) jeśli vtable nie jest unikalne dla wartości — podaj wszystkie typy, których vtable wskazuje ten sam slot-0, i rozróżnij dataflowem; status: CONFIRMED(nazwa klasy + layout) lub RECEIVER_UNRESOLVED z granicą.
2. INSERT-SEAM MAPY (producent/writer, także omijający FUN_00845F70): (a) zmapuj strukturę managera (ctor FUN_008550C0; 3×CS; mapa head@+0x30/+0x34/+0x38 — jaki to kontener: std::map/rb-tree/hash?), wskaż funkcję INSERT (szukaj zapisów do [mgr+0x10]/[mgr+0x30-0x38] i wywołań allocatorów rb-tree — STLport; census E8 do kandydatów insert); (b) prześledź KAŻDEGO realnego writera: skąd bierze klucz i wartość (argumenty/upstream); (c) rozdziel: insert pierwotny (deserializacja/ładowanie) vs copy/replace/propagacja (np. FUN_00845F70 jest writerem atrybutu — czy on też woła insert? ustal jego dokładną rolę: writer-do-istniejącego-kontenera czy creator); (d) jeśli insert jest wirtualny — podaj slot, możliwe typy, miejsce wymagające rozstrzygnięcia.
3. MODEL KLUCZY ścieżki transformu: (a) w builderze FUN_00567770 i driverze FUN_00567C50 ustal RZECZYWISTE klucze przekazywane do walkerów (FUN_00846840/FUN_0085b840-family/FUN_008544D0) — dekompiluj argumenty: czy klucze to ID typów atrybutów (0x6A4-rodzina), class-ID (20xxx), entity-ID, czy klucze kontenerów; zapisz konkretne wartości imm32 na każdej krawędzi; (b) ustal odbiorców (receivery) na każdej krawędzi: czym jest ECX w każdym CALL gettera (kontener encji? wartość mapy? rekord placementu?); rozróżnij POZIOMY WSKAŹNIKÓW (key→hit→[hit+8]→value); (c) zrób to samo dla trzech napędów buildera (A: FUN_00468910→FUN_0058DB50; B: FUN_005B72C0 [0xB9]; C: FUN_00514EF0) — jakie klucze/receivery każdy napęd przekazuje.
4. ŹRÓDŁO DANYCH INSERTÓW: (a) jeśli insert-seam prowadzi do parserów VFS (FUN_00730c90/FUN_0094bd30/FUN_0094f350-family → FUN_008544D0-mapa) — prześledź dokładnie: który parser, który plik/rodzina, jakie klucze z pliku; (b) jeśli prowadzi do komunikatów (0xB9-handler/executor) — zapisz konkretne read-cursor operacje (FUN_00752700/40/26 40), pola, następne wywołania, granice wirtualne; (c) jeśli oba/więcej — rozdziel pierwotne źródło od propagacji; każda krawędź: PROVEN (VA+bytes+dataflow) lub UNPROVEN (jawna).
5. JEŚLI ślad wskazuje Parameters VFS (decyzja po dowodzie czytnika z zad. 4): wybierz JEDNĄ uzasadnioną rodzinę plików; oddziel kontener ArkVFS (magics ArkVFS01/02, CRC-32; reader FUN_00972DF0; stride 36×ceil((16+size)/36) — reguła skalibrowana w Fazie A na 67 parach) od gramatyki payloadu; zbuduj minimalny parser badawczy w B\00_CONTROL: dokładne rozliczenie bajtów, granice rekordów, pola znane/nieznane; zgodność z czytnikiem klienta (te same pola w tej samej kolejności); negatywy: truncation, uszkodzony count/offset, niewspierana wersja, bądny endian — każdy FAIL-closed; NaN/Inf jawne jeśli nadajesz znaczenie numeryczne. Round-trip opaque-blob ≠ zrozumienie pól — deklaruj poziom.
   Skan kluczy 0x6A4/0x6A5/0x6A8/0x6A9/0x6AC/0x23 w payloadach = TYLKO locator pomocniczy: pozytyw wymaga roli pola potwierdzonej kodem (konsument); negatyw = ANCHORS_ABSENT_SEMANTICS_OPEN. Nie narzucaj u32 LE ani formatu fixture-table.
6. KANDYDACI RUNDA-1 (F4): dla FUN_00567170, FUN_005B5F90, FUN_006CB6F0 — spróbuj połączyć identyfikator definicji → konkretny rekord derived → instancję modelu PRZEZ TEN SAM pointer/key i jawne transfery (nie przez sąsiedztwo adresów). QC Fazy A ustaliło: FUN_00567170 ma ZERO attr-callow we własnym ciele, woła singleton FUN_004154F0 (param-manager getter) i settery f60/f90/fb0 — prześledź, skąd bierze wartości setterów (mapa parametrów? z jakim kluczem?) i czy ten sam klucz identyfikuje definicję modelu (A/296445). Jeśli brakuje targetu virtual-call — pokaż slot, możliwe typy, miejsce rozstrzygnięcia.
7. TRANSFORM SEMANTICS DISCIPLINE: rozdziel model-local / parent-local/cell-relative / world transform; wymagaj konsumenta + reguły kompozycji zanim osi/jednostek/offsetu regionu. DLA 4508/296445 NIE twórz przykładowych XYZ jako "odzyskanej lokalizacji" — synthetic instance = test mechanizmu, nigdy placement historyczny. Jeśli jakaś wartość wygląda na XYZ — traktuj jako hipotezę do czasu konsumenta.

KONTROLE OBOWIĄZKOWE: każdy VA → własne mapowanie → bajty → granice instrukcji; kontrola pozytywna (łańcuch rekordu templates.vfs z Fazy A jako wzorzec poprawnej proweniencji) + kontrola odróżniająca (ta sama operacja na innym typzie); drugi rzeczywisty przypadek dla każdego twierdzenia ogólnego; censusy z pełnymi listami VA w JSON; determinizm (SCRIPT_SHA256.csv; hashe po ostatniej edycji; brak timestampów); trzy poziomy atrybucji rozdzielone (własne wykonanie / odczyt cudzego artefaktu / cudza deklaracja).

BRAMKI (STAGE_ACCEPTANCE_GATES.csv, fail-closed, MEASURED_QUANTITY + INDEPENDENT_SOURCE + WHY_NON_CIRCULAR + FAILURE_CASE_DETECTED):
- GB1-VALUE-CLASS: klasa wartości mapy zidentyfikowana (RTTI+vtable+ctor) LUB RECEIVER_UNRESOLVED z dokładną granicą.
- GB2-INSERT-SEAM: insert/writer mapy VA-locked (fun, argumenty, źródło klucza/wartości) LUB SEAM_UNRESOLVED z granicą; rozliczony każdy realny writer.
- GB3-KEY-MODEL: klucze/receivery na ścieżce transformu (builder+3 napędy) rozpisane per krawędź (PROVEN/UNPROVEN).
- GB4-DATA-SOURCE: krawędź źródła danych insertów (VFS-parser LUB komunikat LUB inne) prześledzona do granicy; nie sugeruj źródła bez pozytywu.
- GB5-PARSER (tylko jeśli dekodujesz rodzinę VFS): dokładne rozliczenie bajtów + zgodność z czytnikiem + negatywy FAIL-closed + pola UNKNOWN jawne.
- GB6-IMMUTABLE: census hashy historycznych pakietów (w tym pakiet A) przed/po — zero zmian.
- GB7-ERA: S0 fail-closed.
NON-PASS dozwolone: FORMAT_UNDECODABLE / RECEIVER_UNRESOLVED / SEAM_UNRESOLVED / ANCHORS_ABSENT_SEMANTICS_OPEN — każde z granicą ("co trzeba, żeby rozstrzygnąć").
HARD STOPS: runtime/sieć/mock/Frida; modyfikacja oryginałów/historycznych pakietów/AUDIT_ENTRYPOINT.md; dotknięcie experiments/ lub src/game/; kolizja katalogu; SHA mismatch; sub-agenty; operacje git.

ODDZIAŁ (kompatkty handoff): AUDIT_OUTPUT_ROOT / FINAL_REPORT_PATH (06_REPORT\RESEARCH_FINDINGS.md) / PRIMARY_EVIDENCE_PATHS / RUN_STATUS (PASS / PASS_WITH_BOUNDARY / NON-PASS z klasami) / HARD_STOP_REASON / granice (RECEIVER_UNRESOLVED, SEAM_UNRESOLVED itp.) / jedno-zdaniowa odpowiedź na GŁÓWNE PYTANIE na dziś / następny eksperyment. Nie ogłaszaj STATIC_INSTANCE_MECHANISM_CONFIRMED ani źródła placementu bez dowodu; pozycje 296445 pozostają NIEODZYSKANE dopóki ich nie odzyskasz (a wtedy — z niezależnym holdoutem).
