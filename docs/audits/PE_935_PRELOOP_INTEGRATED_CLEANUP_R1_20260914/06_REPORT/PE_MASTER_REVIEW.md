PE_MASTER_REVIEW
AUDITED_RUN      = PE_935_PRELOOP_INTEGRATED_CLEANUP_R1_20260914 (BASE a7a6c756bc35a5b28220236e9ac649131206aeb3; executor pe-reconstruction W1-W9; formalization + fresh G14 QC by pe-master-auditor sessions; commit range = the persistence commits of this run)
VERDICT          = MASTER_ACCEPTED (advisory)
AUTHORITY_STATUS = ADVISORY_PRE_QUALIFICATION (PROVISIONAL_UNTIL_QUALIFIED; no human Q1 PASS recorded)
CANONICAL_GATE_EFFECT = NONE
SNAPSHOT_STATE   = Audit start: HEAD == origin/master == ls-remote == a7a6c756 (BASE); untracked == FIRSTCALL pkg + cleanup pkg + experiments/; SLOT17 branch tip local==remote==5290e79, parent 3644e5ac, worktree clean. Executor: ZERO git mutations (asserted at start/end + QC). Final state: the persistence commits (this run's G15) on top of a7a6c756; post-push verification in the delivery notice + the PE-MASTER final loop report.
COVERAGE         = FULLY READ by PE-MASTER: the run contract (all sections); CANONICAL_STATE_RECONCILIATION.md (all, incl. the R3 draft); REPORT.md (all); HANDOFF.md (all); STAGE_ACCEPTANCE_GATES.csv (all rows); QC_AUDIT.md (all 429 lines — the QC's own coverage and sources audited); EBP_CLASSIFIER_RULE_V2.md (all); FIRSTCALL SLOT17_BODY_RAW.txt (all); the E1/E2 census CSV rows + raw why-lines (grep-verified); the fired-reason why-line samples (R-ESP/R-STACK-PTR/R-CTOR-OTHER). CENSUS-LEVEL: the entrypoint rows; the SLOT17 package 35-file census (own listing + QC ls-tree); AUD-F3 mtimes (own listing). PHYSICAL (AUDITOR_COUNTERCHECK, own byte reads of the SHA-pinned EXE): the PE optional header (e_lfanew 0x120, opt 0x138, magic 0x10B, ImageBase@+0x1C=0x00400000, SectionAlignment@+0x20=0x1000, DllChar@+0x46=0 — the AUD-F4 refutation); E1 chain anchors (0x007EAC00 lead 00 00; 0x007EA740 entry: CC padding + SEH prologue 6A FF 68 8C 12 A2 00 64 A1...; 0x007EA76D 8B AC 24 A0 01 00 00 = mov ebp,[esp+0x1a0]; 0x007EB1B3 89 75 30; the FNMAP defect pad bytes: 0x90 @0x007EABFF = the DISP of fld [esp+0x90] D9 84 24 90 00 00 00, 0x90 @0x0082DABF = the DISP of add [ebp+0x90],esi 01 B5 90 00 00 00 — operand bytes misread as padding, the attribution-defect mechanism byte-proven); E2 chain anchors (0x0082DAC0 lead 00 00; 0x0082DA80 53 55 8B E9 = push ebx; push ebp; mov ebp,ecx; 0x0082DB61 66 89 4D 30); the NiRTTI thunks (NiNode @0x00A6C200: 68 70 72 BA 00 68 00 CE A8 00 B9 18 72 BA 00 E8 CC D7 CA FF — push 0xBA7270, push "NiNode"@0xA8CE00, mov ecx 0xBA7218, call 0x7199E0; NiAVObject @0x00A6C330: 68 24 72 BA 00 68 BC D8 A8 00 B9 70 72 BA 00 E8 9C D6 CA FF C3 — push 0xBA7224, push "NiAVObject"@0xA8D8BC, mov ecx 0xBA7270, call 0x7199E0, ret; the literal "NiAVObject" ASCII-verified at 0x00A8D8BC — the two initializers MUTUALLY CORROBORATE); the GB12 NiNode vtable dump JSON slots 13-19 (13 GetGroup / 14 SetGroup / 15 UpdateControllers / 16 UpdateNodeBound / 17 ApplyTransform / 18 GetObjectByName / 19 SetSelectiveUpdateFlags — the prose-table defect confirmed against the raw record). NOT_CHECKED by PE-MASTER personally: the full EOF reads of the 01_RAW disassembly listings (the load-bearing anchors re-measured personally + independently by the QC); the census.py source in full (the QC read the classification/FNMAP logic; PE-MASTER corroborated the mechanism with own byte proofs + why-line samples); the remaining 15 SLOT17 package files not cited by the findings (covered by the QC's 4-file spot-hash vs its MANIFEST + the immutability asserts).
STATUS_ALGEBRA   = CLAIM_KNOWLEDGE_STATUS: E1 reclassification REJECTED->POSSIBLE_ALIAS = CONFIRMED (three lineages: executor, fresh QC, PE-MASTER byte anchors; the historical R-EBP-INHERITED rule UNSOUND + both historical function attributions DISPROVEN); E2 REJECTED_STANDS_SOUND (case B + ArkNiTGAReader class corroboration) = CONFIRMED (three lineages); canonical counts 3643 = 2 PROVEN / 620 POSSIBLE / 3021 REJECTED / 0 UNRESOLVED = CONFIRMED (derived, not hard-coded; TOTAL invariant asserted; CSV/RAW byte-identical 71552E2A.../64402A73...); classifier rule V2 + the mandatory negative test (unknown live-in EBP -> POSSIBLE_ALIAS) = CONFIRMED (QC re-run 1:1); NiRTTI 0x00BA7270 semantic identity = NiAVObject NiRTTI CONFIRMED = JUSTIFIED (the human-authorized bounded probe; sole B9 70 72 BA 00 hit @0x00A6C33A; the same evidence class as the standing 0xBA7218 CONFIRMED; storage is the .data virtual-only tail — disclosed); F5_PROCESS_STATUS = PARKED_UNAUTHORIZED_ATTEMPT (human OPTION C, recorded verbatim-faithful) + F5_SCIENCE_STATUS = SUPERSEDED_SCIENTIFICALLY_BY PE_935_NINODE_SLOT17_GAMEBRYO_ORACLE_MINICHECK_R1_20260914 (5290e79) = CONFIRMED (executor + QC + PE-MASTER own comparison: FIRSTCALL's measured facts 6/6 consistent with and strictly subsumed by SLOT17's independent re-measurements; FIRSTCALL itself remains NOT an evidence source and untouched 7/7); AUD-F1..F7 dispositions = CONFIRMED (each re-derived by the executor + QC + PE-MASTER spot checks; F1/F7 temporal-scope errata with RUN_START true / PUBLICATION drift (reflog 3644e5ac@10:12:52 -> a7a6c756@14:40:54 vs branch commit 15:11:43) and REMOTE_HEAD_CHRONOLOGY = UNRESOLVED; F2 census 03_EVIDENCE=16/06_REPORT=4 TOTAL 35; F3 exactly 5 carried control/locator files, evidence classes regenerated, no science downgrade; F4 "+4 shifted" REJECTED — standard IMAGE_OPTIONAL_HEADER32 layout holds; F5 "Gb112_eval full same content" REJECTED — Gb112_eval = Documentation only, oracle identity = installed Evaluation SDK NiMain.lib size 3073590 SHA FF4519AF...; F6 superseded chat wording recorded without reconstruction). FINDING_DISPOSITION: P2-1 ACCEPTED as a CONFIRMED input-layer defect of the HISTORICAL LINK30 census (not of this cleanup), rule-level scoped: R-ESP 2765 rows FORM_ONLY (attribution-independent — PE-MASTER corroborated via own why-line samples), up to 257 REJECTED rows ATTRIBUTION_DEPENDENT (R-STACK-PTR 129 / R-CTOR-OTHER 104 / R-ZERO 18 / R-LEA-STACK 3 / R-EBP-INHERITED 2 / R-CONT-FIELD 1; R-IMM-STATIC 1 already superseded), QC sample N=15 = 15/15 plausible starts (a SAMPLE, not a census; population exposure UNKNOWN). EXECUTABLE_GATE_STATE: G0-G13 PASS (executor self-assessment, QC-verified); G8 both REQUIRED classes returned; G14 = QC_PASS_WITH_FINDINGS; G15 = this persistence. HUMAN_REVIEW_STATE: the human's §27 pre-loop gate = PRELOOP_CLEANUP_STATUS ACCEPTED by PE-MASTER adjudication (advisory); milestone closure NOT in scope. PERSISTENCE_STATE: the executor zero-mutation phase verified; publication = this commit set. APPLICATION_STATE: the errata/sidecars applied as specified (the R3 sidecar, the entrypoint rows, the SLOT17 review — the persistence worker's step). MILESTONE_STATE: EU935-M1 unchanged; no closure, no next milestone.
CLAIM_MATRIX     = (1) E1 0x007EB1B3 REJECTED->POSSIBLE_ALIAS — evidence: own bytes 0x007EAC00/0x007EA740/0x007EA76D/0x007EB1B3 + the executor censuses (zero refs to the false attribution; sole E8 caller 0x007EB284; wrapper 3 call sites; arg3 = ECX-this/stack-arg/caller-EBP) + QC reproduction — CONFIRMED; NOT a claim that the write IS SF+0x30 (NONE_PROVEN is the honest class). (2) E2 REJECTED_STANDS_SOUND case B — evidence: own bytes + both-entry-paths stack-local construction (lea ecx,[esp+0x9c]; ctor 0x82d920; vtable 0x00A9189C -> .?AVArkNiTGAReader@@) — CONFIRMED. (3) Counts 2/620/3021/0 — CONFIRMED (CSV self-count + AMEND_R2 delta + E1 delta; TOTAL 3643 asserted thrice). (4) Classifier V2 negative test — CONFIRMED (QC re-run; the checker demonstrably refuses the unsound REJECTED). (5) NiRTTI upgrade — CONFIRMED (bounded probe + mutual corroboration; the human's §14 authorization respected; the seam did not expand). (6) F5 HOLD + supersession — CONFIRMED. (7) AUD-F1..F7 — CONFIRMED as dispositions with own evidence. (8) The P2-1 exposure statement — CONFIRMED at rule level; the per-row truth of the remaining 255 exposed rows = UNKNOWN (honestly recorded; conservative-direction only for the load-bearing census claims: PROVEN rows are anchor-based; false REJECTEDs can only fall to POSSIBLE, never silently to PROVEN).
GATE_PREDICATES  = G8: the REQUIRED classes (negative POSSIBLE_ALIAS, positive REJECTED_ALIAS) asserted by execution, not prose — necessary (the rule V2 semantics) and sufficient (the implementation is the rule's operational form); false-pass case: a checker hardcoding POSSIBLE for everything would pass the negative but fail the positive control — the pair closes it. G9: TOTAL==3643 + per-row OLD/NEW/PROOF + CSV/RAW hash-identity — necessary and sufficient for the recomputation claim; false-fail case: none (the counts are derived, and the derivation chain is byte-anchored). G6/G7: the A/B/C/NONE_PROVEN case recording with VA+bytes — necessary for the classification; sufficient within the declared two-level bound (the bound is the declared limit, honestly recorded). G5: the three-part NiRTTI statement with the upgrade explicitly tied to the bounded probe — necessary and sufficient for the CONFIRMED class as defined (call-site proof). Coverage gaps: the 255 non-sampled exposed rows (the P2-1 residue) — declared, queued, non-blocking for THIS run's claims.
CODE_FINDINGS    = NONE in this run's generators (the classifier implements rule V2 correctly per the QC source review + re-run; the probes are fail-closed on the SHA pin). HISTORICAL code finding (input-layer, not this run's code): the LINK30 census.py FNMAP attribution heuristic treats operand/disp 0x90/0xCC bytes as entry padding — byte-proven at 0x007EABFF (fld [esp+0x90] disp) and 0x0082DABF (add [ebp+0x90] disp) — P2-1, corrections queued for a future authorized census-wide re-derivation; the historical artifacts stay immutable by design.
EVIDENCE_FINDINGS= the E1/E2 raw listings carry the disproven historical attributions in their FILE NAMES (E1_FUN_007EAC00_DISASM.txt etc.) — the contents correctly disprove them (recorded as QC P3-level cosmetics; no ambiguity: the file headers state the disproof). The identity-verification START/END records used two script revisions (disclosed; measurements identical; QC re-measured everything).
CANON_CONFLICTS  = NONE. The canonical LINK30 layer is superseded EXACTLY as designed: one row (0x0040525B, AMEND_R2) + one row (0x007EB1B3, this run) — both via append-only sidecars; the historical CSV/RAW byte-identical; nothing retracted is cited as standing; the SLOT17 science unchanged (identity stays STRONGLY_SUPPORTED; the locked B->A criterion held — no era/generation-qualified oracle experiment was run).
RETRACTIONS      = SUPERSEDED (not retracted): the R-EBP-INHERITED rule (V2 replaces it; the template recorded); the historical function attributions for the two rows (disproven); the SLOT17 HANDOFF "no drift" wording + REPORT "re-verified" wording (temporal-scope errata, AUD-F1/F7); the SLOT17 REPORT "All evidence was regenerated" (AUD-F3: 5 control/locator files were carried); the SLOT17 docstring "+4 shifted" (AUD-F4); the chat-review claims "Gb112_eval full same content" and "FINAL origin/master=3644..., no drift" (AUD-F5/F6 — REJECTED_WITH_EVIDENCE / superseded temporal wording). RETRACTION(A) != CONFIRMATION(NOT-A): E1's POSSIBLE_ALIAS does NOT assert SF+0x30; E2's REJECTED_STANDS is a NEW proof (case B), not the old rule's survival.
PERSISTENCE_CHECK = The executor phase: zero git mutations, HEAD==BASE at every phase (asserted). Publication (this commit set): the cherry-pick of 5290e79 (35 paths, byte-identical package, parent a7a6c756), the cleanup package commit (all files incl. this review, the manifest self-excluded per L12), the persistence-layer commit (R3 sidecar + SLOT17 review + entrypoint +2/-0 rows). Post-push HEAD == origin/master == ls-remote verified by the persistence worker and re-verified by PE-MASTER in the final loop report.
APPLICATION_READY= YES — executed by this persistence step: the R3 sidecar text (the DRAFT-BEGIN/END block applied verbatim); the entrypoint rows (exact content specified in the dispatch); the SLOT17 review (the first on-disk persistence, honestly labeled with its issuance context). Zero unplanned target files.
CHECKPOINT_DELTA = NONE (PE_CURRENT_CHECKPOINT.md is the FOUNDATION-era bootstrap; the run state is carried by AUDIT_ENTRYPOINT.md per convention).
NEXT_EXPERIMENT  = (loop Phase 2, human-authorized, time permitting) P1 ARG2 PROVENANCE — who supplies FUN_0050A050's arg2 (the name argument of the NiNode::GetObjectByName dispatch), what values, what semantic role; statuses ARG2_ABI / ARG2_PROVENANCE / ARG2_VALUE_CLASS / ARG2_FINAL_SEMANTIC_ROLE independent; hypotheses (node/bone/socket/marker names) stay hypotheses. Secondary P2: the downstream world-position consumer (+0x90 reads after the dispatch). The P2-1 census-wide attribution re-derivation (up to 257 rows) is QUEUED as project backlog for a future authorized run — NOT this loop.
ORDERED_WORK     = This persistence step (pe-master-auditor): the completion batch (EBP_REVALIDATION.md per the human's §24 structure; the HANDOFF raw-file count fix; the dispositions cross-reference fix; the gates G14/G15 closure; the REPORT §11 field updates) -> the manifest -> the cherry-pick -> the R3 sidecar -> the SLOT17 review -> the entrypoint rows -> the three path-limited commits + push + verification. NO other writes.
HANDOFF_BLOCK    = AUDIT_OUTPUT_ROOT: docs/audits/PE_935_PRELOOP_INTEGRATED_CLEANUP_R1_20260914/; FINAL_REPORT_PATH: its 06_REPORT/REPORT.md + 06_REPORT/PE_MASTER_REVIEW.md; PRIMARY_EVIDENCE_PATHS: 01_RAW/E1_FUN_007EAC00_DISASM.txt, 01_RAW/E1_CALLER_PROVENANCE_CENSUS.txt, 01_RAW/E2_FUN_0082DAC0_DISASM.txt, 01_RAW/E2_CALLER_PROVENANCE_CENSUS.txt, 01_RAW/EBP_CLASSIFIER_TEST_OUTPUTS.txt, 01_RAW/NIRTTI_BOUNDED_PROBE_0xBA7270.txt, 01_RAW/ENTROPIA_PE_OPTIONAL_HEADER_DUMP.txt, 02_ANALYSIS/CANONICAL_STATE_RECONCILIATION.md (incl. the R3 draft), 02_ANALYSIS/EBP_REVALIDATION.md, 02_ANALYSIS/SLOT17_ERRATA.md, 02_ANALYSIS/SLOT17_AUDIT_FINDINGS_DISPOSITION.md, 02_ANALYSIS/F5_DISPOSITION.md, 02_ANALYSIS/SCIENCE_STATUS_MATRIX.csv, 06_REPORT/QC_AUDIT.md; RUN_STATUS: COMPLETE (PRELOOP_CLEANUP_STATUS = ACCEPTED); HARD_STOP_REASON: NONE for this run (the loop continues to Phase 2 science under the human's authorization).
