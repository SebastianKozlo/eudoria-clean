# EVIDENCE INDEX — PE_935_PRELOOP_INTEGRATED_CLEANUP_R1_20260914

RUN_ID: PE_935_PRELOOP_INTEGRATED_CLEANUP_R1_20260914 · Era: PCG_9_3_5 · MODE: STATIC-ONLY
(the client binary was NEVER executed; all analysis = own byte reads + own capstone
disassembly of the pinned physical Entropia.exe, SHA256
E7785430E81DFFE648CE8F5312414B17BC9FCE61389689A22F753765D5280F31, SIZE 8015872,
fail-closed asserted before any byte read in every probing script).
Machine index: EVIDENCE_INDEX.csv (same directory). All executor outputs are NEW
untracked files inside this package only; ZERO git mutations; historical artifacts
(SLOT17 commit/branch/worktree package, LINK30 CSV/RAW, FIRSTCALL package, experiments/)
were READ-ONLY with byte-identity asserted before == after.

## How to read this package

1. `00_CONTROL/RUN_CONTRACT.md` (the governing contract), `00_CONTROL/SOURCE_IDENTITIES.json`
   (fail-closed pins), `00_CONTROL/GIT_OBSERVATIONS_AT_FORMALIZE.md` (formalizer snapshot).
2. `01_RAW/` — raw evidence (every file records its generator script + generator SHA256,
   the executed command, the input identities, and the dynamically measured toolchain
   provenance: interpreter 3.12.7; capstone.__version__ 5.0.7 with
   capstone.__file__ (the dist-info label "capstone-5.0.9" is a KNOWN FALSE LABEL — never cited)).
3. `02_ANALYSIS/` — the analysis documents (rule V2, reconciliation, dispositions, errata,
   matrix).
4. `06_REPORT/` — the drafted report, handoff and gates (G14/G15 PENDING — closed by the
   fresh QC and PE-MASTER adjudication, not by this executor).

## Provenance note (disclosed)

`01_RAW/IDENTITY_VERIFICATION_AT_START.txt` was generated by an earlier revision of
`00_CONTROL/cleanup_verify_identities.py` (SHA256 83B392E3..., recorded in that file's
header — the revision fixed a pin-comparison string-format issue so the same byte values
compare as integers); the END record was generated by the current revision (SHA256
1E28AF12..., END mode). BOTH runs measured IDENTICAL values and reported ALL PIN CHECKS
MATCH; the intermediate revision only changed comparison formatting and added the END
output mode. No measurement differs between the revisions.
